export interface BlogPost {
  slug: string;
  title: string;
  description: string;
  date: Date;
  category: string;
  readTime: string;
}

export interface BlogSection {
  heading: string;
  body: string;
}

export interface BlogContent {
  title: string;
  description: string;
  date: Date;
  sections: BlogSection[];
}

export const blogPosts: BlogPost[] = [
  {
    slug: "json-formatting-guide",
    title: "How to Format JSON Properly: A Complete Guide",
    description: "Learn how to format JSON data correctly with best practices, common patterns, and troubleshooting tips for developers.",
    date: new Date("2025-12-15"),
    category: "Formatters",
    readTime: "5 min read",
  },
  {
    slug: "common-json-errors",
    title: "Common JSON Errors and How to Fix Them",
    description: "A comprehensive guide to the most frequent JSON errors developers encounter and step-by-step solutions to fix them.",
    date: new Date("2025-12-10"),
    category: "Formatters",
    readTime: "6 min read",
  },
  {
    slug: "url-encoding-decoding",
    title: "URL Encoding and Decoding: What Every Developer Should Know",
    description: "Understand how URL encoding works, why it matters for web development, and how to properly encode and decode URLs.",
    date: new Date("2025-12-05"),
    category: "Encoders",
    readTime: "4 min read",
  },
  {
    slug: "base64-encoding-methods",
    title: "Base64 Encoding: Methods, Use Cases, and Best Practices",
    description: "A deep dive into Base64 encoding, when to use it, common pitfalls, and how it works under the hood.",
    date: new Date("2025-11-28"),
    category: "Encoders",
    readTime: "5 min read",
  },
  {
    slug: "password-security-best-practices",
    title: "Password Security Best Practices for 2026",
    description: "Learn how to create and manage secure passwords, understand password strength metrics, and protect against common attacks.",
    date: new Date("2025-11-20"),
    category: "Security",
    readTime: "7 min read",
  },
  {
    slug: "color-converter-guide",
    title: "Color Converter Guide: HEX, RGB, and HSL Explained",
    description: "A complete guide to understanding color models in web development, and how to convert between HEX, RGB, and HSL formats.",
    date: new Date("2025-11-15"),
    category: "Converters",
    readTime: "4 min read",
  },
  {
    slug: "uuid-guide",
    title: "UUIDs Explained: What They Are and How to Use Them",
    description: "Everything you need to know about UUIDs — how they work, different versions, and when to use each type in your applications.",
    date: new Date("2025-11-10"),
    category: "Generators",
    readTime: "5 min read",
  },
  {
    slug: "online-tools-security",
    title: "Understanding Online Tool Security: Why Client-Side Matters",
    description: "Discover the security implications of using online developer tools and why client-side processing is crucial for data privacy.",
    date: new Date("2025-11-05"),
    category: "Security",
    readTime: "6 min read",
  },
  {
    slug: "json-vs-xml",
    title: "JSON vs XML: Which Data Format Should You Use in 2026?",
    description: "Compare JSON and XML data formats for APIs, configuration, and data exchange. Learn when to use JSON vs XML and why JSON dominates modern web development.",
    date: new Date("2026-03-01"),
    category: "Formatters",
    readTime: "5 min read",
  },
  {
    slug: "json-schema-guide",
    title: "JSON Schema Guide: Validate Your JSON Data Structure",
    description: "Learn JSON Schema fundamentals — how to define validation rules, enforce data types, and ensure your JSON data meets structural requirements.",
    date: new Date("2026-02-15"),
    category: "Formatters",
    readTime: "6 min read",
  },
  {
    slug: "how-apis-use-json",
    title: "How APIs Use JSON: REST, GraphQL, and WebSocket Data Formats",
    description: "A developer-focused guide to how modern APIs use JSON for request/response payloads, error handling, pagination, and real-time communication.",
    date: new Date("2026-01-20"),
    category: "Formatters",
    readTime: "7 min read",
  },
  {
    slug: "hash-generator-guide",
    title: "Hash Generator Guide: MD5, SHA-1, SHA-256, and SHA-512 Explained",
    description: "Learn how cryptographic hash functions work, the differences between MD5, SHA-1, SHA-256, and SHA-512, and when to use each.",
    date: new Date("2026-04-01"),
    category: "Security",
    readTime: "6 min read",
  },
  {
    slug: "sql-formatting-guide",
    title: "SQL Formatter Guide: Write Clean, Readable SQL Queries",
    description: "Learn how to format SQL queries for better readability, maintainability, and team collaboration.",
    date: new Date("2026-04-05"),
    category: "Formatters",
    readTime: "5 min read",
  },
  {
    slug: "xml-formatting-guide",
    title: "XML Formatter Guide: How to Format, Validate, and Beautify XML",
    description: "Master XML formatting with best practices for indentation, validation, and comparing XML vs JSON.",
    date: new Date("2026-04-10"),
    category: "Formatters",
    readTime: "6 min read",
  },
  {
    slug: "yaml-converter-guide",
    title: "YAML Converter Guide: Convert Between YAML and JSON",
    description: "Learn how to convert YAML to JSON and JSON to YAML, understand YAML syntax, and when to use each format.",
    date: new Date("2026-04-15"),
    category: "Converters",
    readTime: "5 min read",
  },
  {
    slug: "html-entities-guide",
    title: "HTML Entities Guide: Encode Special Characters for Safe Web Rendering",
    description: "Learn how HTML entities work, why they matter for security, and how to encode and decode special characters.",
    date: new Date("2026-04-20"),
    category: "Encoders",
    readTime: "5 min read",
  },
  {
    slug: "case-converter-guide",
    title: "Case Converter Guide: camelCase, snake_case, kebab-case and More",
    description: "Master text case conversion between camelCase, PascalCase, snake_case, kebab-case, and other naming conventions.",
    date: new Date("2026-04-25"),
    category: "Text",
    readTime: "5 min read",
  },
  {
    slug: "markdown-guide",
    title: "Markdown Preview Guide: Write and Preview Markdown Like a Pro",
    description: "Learn Markdown syntax from basics to advanced — headings, lists, tables, code blocks, and more.",
    date: new Date("2026-05-01"),
    category: "Text",
    readTime: "6 min read",
  },
  {
    slug: "text-diff-guide",
    title: "Text Diff Guide: Compare Texts and Find Differences Like a Developer",
    description: "Learn how text diff checkers work for comparing code and documents, and how to interpret diff output.",
    date: new Date("2026-05-05"),
    category: "Text",
    readTime: "5 min read",
  },
  {
    slug: "image-converter-guide",
    title: "Image Converter Guide: Convert Between PNG, JPEG, WebP, and GIF",
    description: "Learn how to convert images between formats in your browser, and compare PNG vs JPEG vs WebP.",
    date: new Date("2026-05-10"),
    category: "Images",
    readTime: "5 min read",
  },
  {
    slug: "lorem-ipsum-guide",
    title: "Lorem Ipsum Generator Guide: Generate Placeholder Text for Designs",
    description: "Learn how to use Lorem Ipsum placeholder text in web design, print layout, and UI mockups.",
    date: new Date("2026-05-15"),
    category: "Generators",
    readTime: "4 min read",
  },
  {
    slug: "jwt-authentication-explained",
    title: "JWT Authentication Explained: How JSON Web Tokens Work",
    description: "A complete guide to JWT authentication — learn how JSON Web Tokens work, their structure, how they secure APIs, and best practices for implementation.",
    date: new Date("2026-06-01"),
    category: "Security",
    readTime: "7 min read",
  },
  {
    slug: "json-vs-yaml-for-apis",
    title: "JSON vs YAML for APIs: Choosing the Right Data Format",
    description: "Compare JSON and YAML for API development — learn when to use each format, their strengths and weaknesses, and best practices for API design.",
    date: new Date("2026-06-05"),
    category: "Converters",
    readTime: "6 min read",
  },
  {
    slug: "uuid-v4-vs-uuid-v7",
    title: "UUID v4 vs UUID v7: Choosing the Right UUID for Your Application",
    description: "Compare UUID v4 and UUID v7 — understand the differences between random and time-ordered UUIDs, database performance implications, and when to use each version.",
    date: new Date("2026-06-10"),
    category: "Generators",
    readTime: "5 min read",
  },
  {
    slug: "regex-tutorial",
    title: "Regex Tutorial for Beginners: Learn Regular Expressions with Examples",
    description: "A complete regex tutorial for beginners. Learn how to use regular expressions for pattern matching, text search, and data validation with practical examples.",
    date: new Date("2026-06-14"),
    category: "Text",
    readTime: "6 min read",
  },
  {
    slug: "css-units-guide",
    title: "CSS Units Guide: px, rem, em, vh, vw, and More Explained",
    description: "A complete guide to CSS units. Learn the difference between px, rem, em, vh, vw, and other CSS units. Understand when to use each unit for responsive web design.",
    date: new Date("2026-06-14"),
    category: "Converters",
    readTime: "6 min read",
  },
  {
    slug: "docker-compose-tutorial",
    title: "Docker Compose Tutorial for Beginners: Define and Run Multi-Container Apps",
    description: "Learn Docker Compose from scratch. Define multi-container applications, manage services, networks, and volumes with docker-compose.yml files.",
    date: new Date("2026-06-14"),
    category: "Web",
    readTime: "6 min read",
  },
  {
    slug: "rest-api-design",
    title: "REST API Design Best Practices: Build Better Web APIs in 2026",
    description: "Learn REST API design best practices including resource naming, HTTP methods, status codes, pagination, error handling, and versioning strategies.",
    date: new Date("2026-06-14"),
    category: "Web",
    readTime: "7 min read",
  },
  {
    slug: "how-qr-codes-work",
    title: "How QR Codes Work: A Complete Guide to QR Code Technology",
    description: "Learn how QR codes work — from error correction and data encoding to structure and generation. Understand the technology behind every QR code you scan.",
    date: new Date("2026-06-14"),
    category: "Generators",
    readTime: "5 min read",
  },
  {
    slug: "common-jwt-errors",
    title: "Common JWT Errors and How to Debug Them",
    description: "A practical guide to debugging JWT parsing errors — base64 decoding issues, Bearer prefix problems, signature mismatches, and how to fix them.",
    date: new Date("2026-06-28"),
    category: "Security",
    readTime: "5 min read",
  },
  {
    slug: "json-compact-pretty-print",
    title: "JSON Compact Pretty-Print: Smart Formatting Between Minified and Expanded",
    description: "Learn how smart JSON formatting works — keeping small objects on one line while expanding large ones. A practical guide to compact pretty-print techniques.",
    date: new Date("2026-06-30"),
    category: "Formatters",
    readTime: "4 min read",
  },
  {
    slug: "json-to-csv-guide",
    title: "JSON to CSV Conversion: The Complete Developer's Guide",
    description: "Learn how to convert JSON to CSV in JavaScript, Python, and online tools. Handle nested objects, arrays, and large datasets efficiently.",
    date: new Date("2026-07-01"),
    category: "Formatters",
    readTime: "8 min read",
  },
  {
    slug: "kubernetes-yaml-guide",
    title: "Kubernetes YAML: Writing Production-Grade Manifests",
    description: "A complete guide to writing Kubernetes YAML manifests — Pods, Deployments, Services, ConfigMaps, and validation best practices.",
    date: new Date("2026-07-02"),
    category: "Web",
    readTime: "9 min read",
  },
  {
    slug: "github-actions-guide",
    title: "GitHub Actions CI/CD: From Zero to Production Pipeline",
    description: "Learn GitHub Actions from scratch — workflow syntax, build/test/deploy pipelines, matrix builds, and YAML validation.",
    date: new Date("2026-07-03"),
    category: "Web",
    readTime: "10 min read",
  },
  {
    slug: "jwt-refresh-tokens",
    title: "JWT Refresh Token Rotation: Implementation Guide",
    description: "A practical guide to JWT refresh tokens — token rotation, expiration strategies, secure storage, and preventing replay attacks.",
    date: new Date("2026-07-04"),
    category: "Security",
    readTime: "8 min read",
  },
  {
    slug: "wi-fi-qr-codes",
    title: "Wi-Fi QR Codes: Generate and Share Network Access",
    description: "Learn how Wi-Fi QR codes work, how to generate them for WPA2 and WPA3 networks, and best practices for secure sharing.",
    date: new Date("2026-07-05"),
    category: "Generators",
    readTime: "7 min read",
  },
  {
    slug: "word-counter-guide",
    title: "Word Counter Guide: Track Writing Metrics Like a Pro",
    description: "A complete guide to word counting — why word count matters for SEO, technical writing, and content creation. Learn reading time estimation.",
    date: new Date("2026-07-06"),
    category: "Text",
    readTime: "7 min read",
  },
  {
    slug: "remove-duplicate-lines-guide",
    title: "Remove Duplicate Lines: Complete Developer's Guide",
    description: "Learn how to remove duplicate lines from text — deduplication strategies, sorting, command-line alternatives, and online tools.",
    date: new Date("2026-07-07"),
    category: "Text",
    readTime: "7 min read",
  },
];

export const blogContent: Record<string, BlogContent> = {
  "json-formatting-guide": {
    title: "How to Format JSON Properly: A Complete Guide",
    description: "Learn how to format JSON data correctly with best practices, common patterns, and troubleshooting tips for developers.",
    date: new Date("2025-12-15"),
    sections: [
      { heading: "What is JSON Formatting?", body: "JSON (JavaScript Object Notation) is a lightweight data interchange format that is easy for humans to read and write and easy for machines to parse and generate. Proper JSON formatting involves correctly structuring your data with appropriate indentation, spacing, and syntax to ensure it is both valid and readable." },
      { heading: "Why Proper JSON Formatting Matters", body: "Well-formatted JSON is crucial for debugging, collaboration, and maintaining APIs. Proper formatting helps developers quickly understand data structures, identify errors, and ensure consistent communication between services. Unformatted or minified JSON can hide syntax errors and make debugging significantly more difficult." },
      { heading: "JSON Formatting Best Practices", body: "Always use consistent indentation (2 or 4 spaces), avoid trailing commas, use double quotes for strings and property names, and keep nested structures organized. Our JSON Formatter tool automatically handles all of these best practices for you." },
      { heading: "Common JSON Formatting Errors", body: "Trailing commas, missing quotes, unescaped special characters, and mismatched brackets are among the most common JSON formatting errors. Always validate your JSON after formatting to catch these issues early." },
      { heading: "Using WebUtil's JSON Formatter", body: "Our free online JSON Formatter allows you to format, validate, and beautify your JSON data instantly. Simply paste your JSON, click format, and get properly indented, validated output. All processing happens in your browser — nothing is uploaded to any server." },
    ],
  },
  "common-json-errors": {
    title: "Common JSON Errors and How to Fix Them",
    description: "A comprehensive guide to the most frequent JSON errors developers encounter and step-by-step solutions to fix them.",
    date: new Date("2025-12-10"),
    sections: [
      { heading: "Trailing Commas", body: "One of the most common JSON errors is trailing commas at the end of arrays or objects. Unlike JavaScript, JSON does not allow trailing commas. Remove any extra commas after the last element in an array or property in an object." },
      { heading: "Missing or Incorrect Quotes", body: "JSON requires double quotes around all strings and property names. Single quotes are not valid in JSON. Always use double quotes for consistency and validity." },
      { heading: "Unescaped Special Characters", body: "Characters like newlines, tabs, and backslashes need to be properly escaped in JSON strings. Use \\n for newlines, \\t for tabs, and \\\\ for backslashes to ensure valid JSON." },
      { heading: "Mismatched Brackets", body: "Ensure every opening bracket {, [, or ( has a matching closing bracket }, ], or ). Use a JSON validator to automatically detect mismatched brackets." },
      { heading: "Invalid Data Types", body: "JSON supports strings, numbers, booleans, null, arrays, and objects. Undefined, NaN, Infinity, and functions are not valid JSON values. Ensure all values are valid JSON types." },
    ],
  },
  "json-vs-xml": {
    title: "JSON vs XML: Which Data Format Should You Use in 2026?",
    description: "Compare JSON and XML data formats for APIs, configuration, and data exchange. Learn when to use JSON vs XML and why JSON dominates modern web development.",
    date: new Date("2026-03-01"),
    sections: [
      { heading: "JSON vs XML: Key Differences", body: "JSON (JavaScript Object Notation) and XML (eXtensible Markup Language) are both data interchange formats, but they serve different use cases. JSON is lightweight, uses key-value pairs, and is native to JavaScript. XML is markup language with custom tags, supports attributes, and has mature schema validation. JSON has largely replaced XML in modern web APIs due to its simplicity and native browser support." },
      { heading: "Why JSON Wins for APIs", body: "JSON is the default format for REST APIs, GraphQL, and most modern web services. It parses faster than XML, produces smaller payloads, and integrates seamlessly with JavaScript. Major platforms like Twitter, GitHub, and Stripe use JSON exclusively for their APIs. JSON responses are typically 30-40% smaller than equivalent XML responses, reducing bandwidth costs." },
      { heading: "When XML Still Makes Sense", body: "Despite JSON's dominance, XML remains relevant for document-centric formats like SVG, RSS/Atom feeds, SOAP APIs, and legacy enterprise systems. XML's support for namespaces, attributes, and mixed content makes it better suited for complex document structures. If you need to represent documents with metadata at multiple levels, XML may be the better choice." },
      { heading: "JSON vs XML Performance Comparison", body: "JSON generally outperforms XML in parsing speed and data size. A typical JSON parser can process 2-3x more data per second than an equivalent XML parser. JSON's simpler syntax means less overhead — no closing tags, no attributes, no namespace processing. This makes JSON ideal for high-throughput scenarios like real-time APIs and data pipelines." },
      { heading: "Migration Path: XML to JSON", body: "If you're maintaining legacy XML systems, consider a gradual migration to JSON. Start with new endpoints using JSON responses, add JSON support alongside existing XML endpoints, then migrate internal data processing. Tools like our JSON Formatter can help you inspect and validate JSON data during the transition. Our XML Formatter handles XML-to-JSON conversion when needed." },
    ],
  },
  "json-schema-guide": {
    title: "JSON Schema Guide: Validate Your JSON Data Structure",
    description: "Learn JSON Schema fundamentals — how to define validation rules, enforce data types, and ensure your JSON data meets structural requirements.",
    date: new Date("2026-02-15"),
    sections: [
      { heading: "What is JSON Schema?", body: "JSON Schema is a specification for describing the structure of JSON data. It allows you to define validation rules for your JSON documents, including required fields, data types, allowed values, and nested structures. JSON Schema is written in JSON itself, making it easy to understand and integrate into existing toolchains." },
      { heading: "Why Use JSON Schema?", body: "JSON Schema catches data quality issues before they reach production. It serves as living documentation for your API contracts, enables automatic validation in CI/CD pipelines, and helps generated code and documentation stay in sync. Teams using JSON Schema report 50-70% fewer data-related bugs in production because validation happens at the API boundary." },
      { heading: "JSON Schema Keywords You Need to Know", body: "Core keywords include: type, required, properties, items, minimum/maximum, enum, pattern (for regex), allOf/anyOf/oneOf (for composition), and $ref (for references). The type keyword supports string, number, integer, boolean, array, object, and null. The format keyword provides additional semantic validation like email, uri, date, and regex patterns." },
      { heading: "JSON Schema Example: User Profile", body: "A simple user profile schema might require name (string), email (string with format: email), age (integer between 13 and 120), and roles (array of strings from an allowed set). Our JSON Formatter can help you prepare and validate JSON data against your schemas, though dedicated JSON Schema validators offer full draft-07/2020-12 support." },
      { heading: "JSON Schema Drafts and Tooling", body: "JSON Schema has evolved through drafts 04, 07, and 2020-12. Draft-07 is the most widely supported version in open-source tools. Popular validators include Ajv (JavaScript), jsonschema (Python), and everit-json-schema (Java). Many API design tools like Swagger/OpenAPI and Postman integrate JSON Schema natively for request/response validation." },
    ],
  },
  "how-apis-use-json": {
    title: "How APIs Use JSON: REST, GraphQL, and WebSocket Data Formats",
    description: "A developer-focused guide to how modern APIs use JSON for request/response payloads, error handling, pagination, and real-time communication.",
    date: new Date("2026-01-20"),
    sections: [
      { heading: "JSON in REST APIs", body: "REST APIs use JSON as the primary data format for both requests and responses. A typical REST response includes status, data, and metadata. JSON's lightweight syntax and native browser support make it the default choice for RESTful services. Most major web APIs — from GitHub to Stripe to Google — use JSON as their primary data format." },
      { heading: "JSON in GraphQL APIs", body: "GraphQL uses a JSON-like query syntax and always returns JSON responses. Unlike REST, GraphQL lets clients specify exactly which fields they need, reducing over-fetching and under-fetching of data. GraphQL responses follow a standard envelope pattern with data and errors fields, making error handling predictable and consistent across all queries and mutations." },
      { heading: "JSON API Error Handling Patterns", body: "Standardized error responses help client applications handle failures gracefully. The JSON:API specification defines a standard error format with title, detail, status, and source fields. Many APIs include an errors array in their JSON response body with a code, message, and optional details for each error, making debugging straightforward for client developers." },
      { heading: "JSON Pagination Strategies for APIs", body: "APIs serving large datasets implement pagination to avoid overwhelming clients. Common JSON pagination patterns include cursor-based (using a next_cursor field), offset-based (page/limit parameters), and page-based (page_number/page_size). Each approach has trade-offs for consistency, performance, and ease of client implementation." },
      { heading: "Real-Time JSON with WebSockets", body: "WebSocket APIs use JSON frames for real-time bidirectional communication. JSON's self-describing nature makes it ideal for messaging protocols where different message types carry different data structures. Common patterns include a type field to dispatch messages, a data payload for the actual content, and optional metadata fields for timestamps and correlation IDs." },
    ],
  },
  "url-encoding-decoding": {
    title: "URL Encoding and Decoding: What Every Developer Should Know",
    description: "Understand how URL encoding works, why it matters for web development, and how to properly encode and decode URLs.",
    date: new Date("2025-12-05"),
    sections: [
      { heading: "What is URL Encoding?", body: "URL encoding, also known as percent-encoding, converts characters into a format that can be transmitted over the internet. Special characters are replaced with a % followed by two hexadecimal digits representing the ASCII code of the character." },
      { heading: "Why URL Encoding is Necessary", body: "URLs can only contain certain characters from the ASCII set. Characters like spaces, quotes, and non-ASCII characters must be encoded to ensure reliable transmission. URL encoding ensures that data in URLs is correctly interpreted by web servers." },
      { heading: "Common URL Encoding Examples", body: "Spaces become %20, exclamation marks become %21, and non-ASCII characters like é become %C3%A9. Our URL Encoder tool handles all of these conversions automatically." },
      { heading: "Difference Between encodeURI and encodeURIComponent", body: "encodeURI encodes a complete URI but preserves characters that are part of the URI syntax. encodeURIComponent encodes everything, making it safe for use as a query parameter value." },
      { heading: "URL Decoding Best Practices", body: "Always decode URL parameters on the server side. When building URLs dynamically, always encode user-provided values to prevent URL injection and malformed URLs." },
    ],
  },
  "base64-encoding-methods": {
    title: "Base64 Encoding: Methods, Use Cases, and Best Practices",
    description: "A deep dive into Base64 encoding, when to use it, common pitfalls, and how it works under the hood.",
    date: new Date("2025-11-28"),
    sections: [
      { heading: "What is Base64 Encoding?", body: "Base64 is a binary-to-text encoding scheme that represents binary data in an ASCII string format. It is commonly used to transmit data over media designed to handle textual data, such as email or HTTP." },
      { heading: "Common Use Cases for Base64", body: "Base64 is widely used for embedding images in HTML/CSS, attaching files to emails, storing binary data in JSON, and transmitting data in APIs where binary formats are not supported." },
      { heading: "How Base64 Encoding Works", body: "Base64 converts every 3 bytes of binary data into 4 ASCII characters using a specific alphabet of 64 characters (A-Z, a-z, 0-9, +, /). Padding with = characters is used to ensure the output length is a multiple of 4." },
      { heading: "Base64 Best Practices", body: "Base64 increases data size by approximately 33%, so avoid using it for large data transfers when alternatives exist. Always consider whether Base64 is the right solution, or if a binary format would be more efficient." },
      { heading: "Using WebUtil's Base64 Tool", body: "Our Base64 Encoder/Decoder tool allows you to instantly convert text to and from Base64 format. Everything runs in your browser, ensuring your data never leaves your computer." },
    ],
  },
  "password-security-best-practices": {
    title: "Password Security Best Practices for 2026",
    description: "Learn how to create and manage secure passwords, understand password strength metrics, and protect against common attacks.",
    date: new Date("2025-11-20"),
    sections: [
      { heading: "What Makes a Strong Password?", body: "A strong password is long (at least 12 characters), includes a mix of uppercase letters, lowercase letters, numbers, and symbols, does not contain dictionary words or personal information, and is unique for each account." },
      { heading: "Understanding Password Strength", body: "Password strength is measured by entropy — a measure of unpredictability. Entropy is affected by length and character variety. A password with 80+ bits of entropy is generally considered secure against offline attacks." },
      { heading: "Common Password Attacks", body: "Brute force attacks try every possible combination. Dictionary attacks use common words and patterns. Phishing attacks trick users into revealing passwords. Rainbow table attacks use precomputed hash values." },
      { heading: "Password Manager Benefits", body: "Use a password manager to generate and store strong, unique passwords for every account. This eliminates password reuse and makes it practical to use truly random passwords for every service." },
      { heading: "Using WebUtil's Password Generator", body: "Our free Password Generator creates cryptographically secure random passwords with customizable length and character sets. All generation happens in your browser using the Web Crypto API." },
    ],
  },
  "color-converter-guide": {
    title: "Color Converter Guide: HEX, RGB, and HSL Explained",
    description: "A complete guide to understanding color models in web development, and how to convert between HEX, RGB, and HSL formats.",
    date: new Date("2025-11-15"),
    sections: [
      { heading: "Understanding Color Models", body: "Color models are mathematical representations of colors. HEX uses hexadecimal values, RGB uses red/green/blue components, and HSL uses hue/saturation/lightness. Each has its own advantages for different use cases." },
      { heading: "HEX Colors Explained", body: "HEX colors use six hexadecimal digits (#RRGGBB) to represent 16.7 million colors. They are compact and widely used in web development. A three-digit shorthand (#RGB) is also available for certain colors." },
      { heading: "RGB Colors Explained", body: "RGB uses three values from 0-255 representing red, green, and blue components. It's the native color model for computer displays and is intuitive for specifying exact color values." },
      { heading: "HSL Colors Explained", body: "HSL represents colors by hue (0-360 degrees on the color wheel), saturation (0-100%), and lightness (0-100%). HSL is more intuitive for creating color schemes because it separates the color from its intensity." },
      { heading: "Color Accessibility and Contrast", body: "When designing for the web, ensure sufficient contrast between text and background colors. The WCAG guidelines require a contrast ratio of at least 4.5:1 for normal text and 3:1 for large text." },
    ],
  },
  "uuid-guide": {
    title: "UUIDs Explained: What They Are and How to Use Them",
    description: "Everything you need to know about UUIDs — how they work, different versions, and when to use each type in your applications.",
    date: new Date("2025-11-10"),
    sections: [
      { heading: "What is a UUID?", body: "A UUID (Universally Unique Identifier) is a 128-bit identifier standardized by RFC 4122. UUIDs are designed to be unique across space and time, making them ideal for distributed systems where IDs must not conflict." },
      { heading: "UUID v4 — Random UUIDs", body: "UUID v4 is the most commonly used version. It generates IDs using random numbers, with 122 bits of randomness. The probability of collision is extremely low, making it suitable for most applications." },
      { heading: "UUID v7 — Time-Ordered UUIDs", body: "UUID v7 is a newer version that combines a timestamp with random bits. This makes UUIDs sortable by creation time, improving database index performance compared to v4." },
      { heading: "When to Use UUIDs", body: "UUIDs are ideal for distributed systems, database primary keys, API identifiers, and any scenario where you need globally unique identifiers without a central authority." },
      { heading: "Using WebUtil's UUID Generator", body: "Our free UUID Generator creates UUID v4 identifiers instantly. You can generate single or multiple UUIDs at once, with bulk generation supporting up to 100 IDs at a time." },
    ],
  },
  "online-tools-security": {
    title: "Understanding Online Tool Security: Why Client-Side Matters",
    description: "Discover the security implications of using online developer tools and why client-side processing is crucial for data privacy.",
    date: new Date("2025-11-05"),
    sections: [
      { heading: "The Problem with Server-Side Tools", body: "Many online tools send your data to a server for processing. This means your sensitive data — passwords, API keys, personal information — is transmitted and potentially stored on third-party servers." },
      { heading: "Client-Side Processing Benefits", body: "Client-side processing keeps your data on your device. All computation happens in your browser using JavaScript. No data is uploaded, no logs are kept, and no third-party ever sees your information." },
      { heading: "WebUtil's Privacy Commitment", body: "Every tool on WebUtil runs 100% in your browser. We never upload your data, never track your usage, and never store your information. Your privacy is built into our platform from the ground up." },
      { heading: "How to Verify Client-Side Processing", body: "You can verify client-side processing by using your browser's developer tools. Check the Network tab — if no requests are made when you use a tool, it's running entirely client-side." },
      { heading: "The Future of Online Privacy", body: "As privacy concerns grow, client-side processing is becoming the standard for online tools. WebUtil is committed to this privacy-first approach, ensuring you can use our tools with complete confidence." },
    ],
  },
  "hash-generator-guide": {
    title: "Hash Generator Guide: MD5, SHA-1, SHA-256, and SHA-512 Explained",
    description: "Learn how cryptographic hash functions work, the differences between MD5, SHA-1, SHA-256, and SHA-512, and when to use each hash algorithm.",
    date: new Date("2026-04-01"),
    sections: [
      { heading: "What Is a Cryptographic Hash?", body: "A cryptographic hash function takes an input of any size and produces a fixed-size output called a digest or hash. Hash functions are one-way — you cannot reverse a hash back to the original input. They are deterministic, meaning the same input always produces the same hash. Properties include preimage resistance, second preimage resistance, and collision resistance." },
      { heading: "MD5 — Fast but Broken", body: "MD5 produces 128-bit hashes and was once widely used for file integrity checking. However, MD5 is cryptographically broken — collision attacks are practical and can be computed in seconds on consumer hardware. Never use MD5 for security purposes. It should only be used for non-cryptographic checksums like file deduplication." },
      { heading: "SHA-1 — Deprecated and Unsafe", body: "SHA-1 produces 160-bit hashes and replaced MD5 in many protocols. In 2017, Google demonstrated a practical collision attack (SHAttered). Major browsers now reject SHA-1 certificates. SHA-1 should not be used for any security application. Migrate to SHA-256 or SHA-512 for all use cases." },
      { heading: "SHA-256 and SHA-512 — Current Standards", body: "SHA-256 (256-bit) and SHA-512 (512-bit) are part of the SHA-2 family and are currently considered secure. SHA-256 is widely used in TLS certificates, digital signatures, and blockchain technology. SHA-512 is faster on 64-bit processors and provides a larger hash size. Both are recommended for modern applications." },
      { heading: "Using the WebUtil Hash Generator", body: "Our free Hash Generator lets you compute MD5, SHA-1, SHA-256, and SHA-512 hashes instantly in your browser. All computation uses the Web Crypto API and happens client-side. Your input never leaves your device. Use it to verify file integrity, generate checksums, or experiment with hash functions." },
    ],
  },
  "sql-formatting-guide": {
    title: "SQL Formatter Guide: Write Clean, Readable SQL Queries",
    description: "Learn how to format SQL queries for better readability, maintainability, and team collaboration. Best practices for SQL formatting and beautification.",
    date: new Date("2026-04-05"),
    sections: [
      { heading: "Why SQL Formatting Matters", body: "Well-formatted SQL is easier to read, debug, and maintain. Consistent formatting helps teams collaborate on database queries, reduces syntax errors, and makes it easier to spot logic issues. Poorly formatted SQL with mixed capitalization and no indentation hides bugs and slows down development." },
      { heading: "SQL Formatting Best Practices", body: "Use uppercase for SQL keywords (SELECT, FROM, WHERE, JOIN), lowercase for column and table names. Indent clauses consistently, put each major clause on a new line, and align related items. Use meaningful table aliases and avoid SELECT * in production queries. Break complex queries into CTEs (WITH clauses) for clarity." },
      { heading: "Common SQL Formatting Patterns", body: "Short queries can stay on one line: SELECT name FROM users WHERE active = 1. Longer queries should break after each clause. JOIN conditions can be indented under the JOIN clause. Subqueries should be indented one level. Use AS for column aliases for explicit readability." },
      { heading: "Formatting Different SQL Statements", body: "SELECT, INSERT, UPDATE, DELETE, and CREATE statements each have their own formatting conventions. INSERT statements should list columns explicitly. UPDATE statements should have WHERE clauses on a separate line. Complex JOIN chains benefit from consistent indentation and explicit join type keywords." },
      { heading: "Using the WebUtil SQL Formatter", body: "Our free SQL Formatter beautifies your SQL queries with customizable indentation and keyword casing. Paste your raw SQL and get properly formatted output instantly. All processing happens client-side — your queries never leave your browser." },
    ],
  },
  "xml-formatting-guide": {
    title: "XML Formatter Guide: How to Format, Validate, and Beautify XML",
    description: "Master XML formatting with best practices for indentation, validation, and structure. Learn how XML compares to JSON and when to use each format.",
    date: new Date("2026-04-10"),
    sections: [
      { heading: "What Is XML Formatting?", body: "XML (eXtensible Markup Language) is a markup language for encoding documents in a format that is both human-readable and machine-readable. Proper XML formatting involves consistent indentation, correct nesting, and valid syntax. Unlike JSON, XML supports attributes, namespaces, and mixed content, making it more flexible for complex documents." },
      { heading: "XML vs JSON: Formatting Differences", body: "XML uses opening and closing tags (<tag></tag>) while JSON uses braces and colons. XML supports attributes within tags, which has no direct JSON equivalent. XML is more verbose — a typical XML document is 2-3x larger than the equivalent JSON. XML's schema validation (XSD) is more mature than JSON Schema." },
      { heading: "XML Formatting Best Practices", body: "Use consistent indentation (2 or 4 spaces), include XML declaration at the top, use meaningful tag names, avoid deeply nested structures, and always close all tags. Use self-closing tags for empty elements. Validate your XML against a schema (XSD) when working with structured data exchange." },
      { heading: "Common XML Validation Errors", body: "Mismatched tags, missing closing tags, invalid characters in tag names, improperly nested elements, and duplicate attributes are the most common XML errors. Unlike JSON, XML is strict about closing tags — every opening tag must have a matching closing tag or be self-closing." },
      { heading: "Using the WebUtil XML Formatter", body: "Our free XML Formatter beautifies, validates, and minifies XML documents. Paste your XML and get properly indented output with syntax highlighting. All processing runs client-side in your browser with no data uploads." },
    ],
  },
  "yaml-converter-guide": {
    title: "YAML Converter Guide: Convert Between YAML and JSON",
    description: "Learn how to convert YAML to JSON and JSON to YAML. Understand YAML syntax, indentation rules, and when to use YAML over JSON for configuration files.",
    date: new Date("2026-04-15"),
    sections: [
      { heading: "What Is YAML?", body: "YAML (YAML Ain't Markup Language) is a human-readable data serialization format that relies on indentation for structure. It is commonly used for configuration files, CI/CD pipelines (GitHub Actions, GitLab CI), and infrastructure-as-code (Docker Compose, Kubernetes, Ansible). YAML is more readable than JSON for humans but requires careful indentation." },
      { heading: "YAML vs JSON: Key Differences", body: "YAML uses indentation instead of brackets, supports comments with #, and has more data types including dates and timestamps. YAML is more concise for nested data but whitespace-sensitive — incorrect indentation causes parse errors. JSON is stricter and more predictable, making it better for machine-to-machine communication." },
      { heading: "Converting YAML to JSON", body: "Converting YAML to JSON is straightforward — the hierarchical structure maps directly. Indentation becomes nested objects, YAML arrays with - become JSON arrays, and key-value pairs become JSON properties. The conversion is lossy — YAML features like comments and anchors are not preserved in JSON output." },
      { heading: "Converting JSON to YAML", body: "Converting JSON to YAML produces clean, indentation-based output. JSON objects become YAML mappings, arrays become YAML lists, and strings remain strings. The JSON-to-YAML conversion is lossless — all JSON data is preserved. Our tool handles nested structures and mixed types correctly." },
      { heading: "Using the WebUtil YAML Converter", body: "Our free YAML to JSON Converter lets you convert between YAML and JSON with live preview. Paste data in either format and see the converted output instantly. All processing runs client-side — your data never leaves your browser." },
    ],
  },
  "html-entities-guide": {
    title: "HTML Entities Guide: Encode Special Characters for Safe Web Rendering",
    description: "Learn how HTML entities work, why they matter for security, and how to encode and decode special characters for safe web development.",
    date: new Date("2026-04-20"),
    sections: [
      { heading: "What Are HTML Entities?", body: "HTML entities are special codes that represent characters that have special meaning in HTML. For example, < is written as &lt; and > is written as &gt;. Entities prevent browsers from interpreting special characters as HTML markup. They also enable displaying characters not available on standard keyboards, like &copy; for the copyright symbol." },
      { heading: "Why HTML Encoding Matters for Security", body: "HTML encoding is critical for preventing Cross-Site Scripting (XSS) attacks. When user input is inserted into a web page without proper encoding, attackers can inject malicious scripts. Always encode user-generated content before displaying it. Our HTML Entities tool helps developers safely encode and decode content." },
      { heading: "Common HTML Entities", body: "Common entities include &amp; (&), &lt; (<), &gt; (>), &quot; (\"), &apos; ('), &nbsp; (non-breaking space), &copy; (©), &reg; (®), &trade; (™), &mdash; (—), and &hellip; (…). Named entities are supported for common characters, while numeric entities (&#NNNN;) work for any Unicode character." },
      { heading: "HTML Entities vs URL Encoding", body: "HTML entities and URL encoding serve different purposes. HTML entities encode characters for safe display in HTML documents. URL encoding (percent-encoding) encodes characters for safe transmission in URLs. A space is &nbsp; in HTML but %20 in a URL. Use our HTML Entities tool for HTML and our URL Encoder for URLs." },
      { heading: "Using the WebUtil HTML Entities Tool", body: "Our free HTML Entities Encoder/Decoder converts special characters to HTML entities and back. Paste text, choose encode or decode, and get the result instantly. All processing happens client-side in your browser with no data uploads." },
    ],
  },
  "case-converter-guide": {
    title: "Case Converter Guide: camelCase, snake_case, kebab-case and More",
    description: "Master text case conversion for programming — convert between camelCase, PascalCase, snake_case, kebab-case, and other naming conventions used in code.",
    date: new Date("2026-04-25"),
    sections: [
      { heading: "Why Case Conversion Matters in Programming", body: "Different programming languages and frameworks use different naming conventions. JavaScript uses camelCase for variables, Python uses snake_case, HTML attributes use kebab-case, and C# uses PascalCase for classes. A Case Converter helps you transform text between these formats instantly without manual editing." },
      { heading: "camelCase vs PascalCase vs snake_case", body: "camelCase starts with a lowercase letter and capitalizes each subsequent word (e.g., firstName). PascalCase capitalizes every word (e.g., FirstName). snake_case uses underscores between lowercase words (e.g., first_name). kebab-case uses hyphens (e.g., first-name). UPPER CASE converts everything to uppercase, and Title Case capitalizes the first letter of each word." },
      { heading: "Common Use Cases for Case Conversion", body: "Developers use case converters when migrating code between languages, renaming API response fields, converting database column names to JavaScript properties, creating CSS class names from component names, and standardizing naming conventions across a codebase." },
      { heading: "Programming Language Conventions", body: "JavaScript/TypeScript: camelCase for variables, PascalCase for classes. Python: snake_case for everything. Java: camelCase for methods, PascalCase for classes. Ruby: snake_case. Go: camelCase for exported, PascalCase for unexported. Rust: snake_case. PHP: camelCase for methods, snake_case for functions." },
      { heading: "Using the WebUtil Case Converter", body: "Our free Case Converter instantly transforms text between camelCase, PascalCase, snake_case, kebab-case, UPPER CASE, lower case, and Title Case. Paste your text, select the target format, and get the result instantly. All processing runs client-side." },
    ],
  },
  "markdown-guide": {
    title: "Markdown Preview Guide: Write and Preview Markdown Like a Pro",
    description: "Learn Markdown syntax from basics to advanced — headings, lists, tables, code blocks, and more. How to use a Markdown previewer for real-time editing.",
    date: new Date("2026-05-01"),
    sections: [
      { heading: "What Is Markdown?", body: "Markdown is a lightweight markup language that uses plain text formatting to create structured documents. It was designed to be easy to read and write in its raw form while converting to valid HTML. Markdown is widely used for README files, documentation, forum posts, note-taking, and static site generators." },
      { heading: "Markdown Syntax Basics", body: "Headings use # (## for H2, ### for H3), bold uses **text**, italic uses *text*, links use [text](url), images use ![alt](url), unordered lists use -, ordered lists use 1., inline code uses backticks, and code blocks use triple backticks. Tables use pipes and dashes, and blockquotes use >." },
      { heading: "Advanced Markdown Features", body: "Extended Markdown supports tables, task lists (- [ ]), footnotes, definition lists, strikethrough (~~text~~), emoji (:smile:), and math expressions ($\\LaTeX$). GitHub Flavored Markdown adds syntax highlighting for code blocks with language identifiers like ```javascript." },
      { heading: "Markdown Use Cases in Development", body: "Markdown is the standard for GitHub README files, project documentation (MkDocs, Docusaurus), technical blog posts, API documentation (Swagger/OpenAPI use Markdown for descriptions), note-taking apps (Obsidian, Notion), and static site generators (Astro, Hugo, Jekyll)." },
      { heading: "Using the WebUtil Markdown Preview", body: "Our free Markdown Preview lets you write Markdown and see the rendered HTML output in real-time. Perfect for testing Markdown syntax before publishing. All rendering happens client-side using your browser — no data is uploaded to any server." },
    ],
  },
  "text-diff-guide": {
    title: "Text Diff Guide: Compare Texts and Find Differences Like a Developer",
    description: "Learn how text diff checkers work, common use cases for comparing code and documents, and how to interpret diff output for debugging and code review.",
    date: new Date("2026-05-05"),
    sections: [
      { heading: "What Is a Text Diff?", body: "A text diff (difference) is the result of comparing two sequences of text to find the changes between them. Diff algorithms identify additions, deletions, and modifications at the line and character level. The most common algorithm is the Myers diff algorithm, used by Git and most diff tools." },
      { heading: "Common Diff Use Cases for Developers", body: "Code reviews — compare pull request changes before merging. Debugging — compare working code with broken code to isolate the issue. Configuration management — track changes in config files across environments. Version comparison — see what changed between software versions. Document revision tracking — find edits in text documents." },
      { heading: "How to Read Diff Output", body: "Diff output typically shows two panes side by side or a single unified view. Additions are shown in green (new lines), deletions in red (removed lines), and unchanged lines in gray. Some tools highlight character-level changes within lines for precise comparison." },
      { heading: "Diff in Version Control", body: "Git uses diff extensively — git diff shows unstaged changes, git diff --staged shows staged changes, and git log -p shows the patch for each commit. Understanding diff output is essential for effective Git usage and collaborative development." },
      { heading: "Using the WebUtil Text Diff Checker", body: "Our free Text Diff Checker compares two texts side-by-side and highlights additions, deletions, and changes. Paste your original and modified text, and see the differences highlighted instantly. Perfect for code reviews, document comparison, and debugging. All processing runs client-side." },
    ],
  },
  "image-converter-guide": {
    title: "Image Converter Guide: Convert Images Between PNG, JPEG, WebP, and GIF",
    description: "Learn how to convert images between formats using your browser. Compare PNG vs JPEG vs WebP for quality, file size, and use cases.",
    date: new Date("2026-05-10"),
    sections: [
      { heading: "Image Format Comparison", body: "PNG offers lossless compression with transparency support, ideal for screenshots, logos, and graphics with text. JPEG offers lossy compression with smaller file sizes, ideal for photographs. WebP offers both lossy and lossless compression with transparency, providing 25-35% smaller files than PNG/JPEG. GIF supports animation but has a limited 256-color palette." },
      { heading: "When to Use Each Format", body: "Use PNG for images with text, sharp edges, or transparency needs. Use JPEG for photographs and complex images where small file size matters more than perfect quality. Use WebP for web performance — it delivers the best quality-to-size ratio. Use GIF only for simple animations — consider MP4 or WebM for video content." },
      { heading: "Image Conversion Best Practices", body: "Always keep an original copy before converting. Choose the output format based on your use case, not habit. WebP is supported by all modern browsers and recommended for web use. Resize images before converting for better performance. Lossy formats like JPEG lose quality each time they are re-saved." },
      { heading: "Browser-Based Image Processing", body: "Modern browsers can process images using the Canvas API without uploading them to a server. This means your images remain private and processing is instant. The Canvas API supports reading PNG, JPEG, WebP, GIF, and BMP formats and exporting to PNG, JPEG, and WebP." },
      { heading: "Using the WebUtil Image Converter", body: "Our free Image Converter converts images between PNG, JPEG, WebP, and GIF formats using your browser's Canvas API. No uploads — your images stay on your device. Select a file, choose the output format, and download the converted image instantly." },
    ],
  },
  "lorem-ipsum-guide": {
    title: "Lorem Ipsum Generator Guide: Generate Placeholder Text for Designs",
    description: "Learn how to use Lorem Ipsum placeholder text in web design, print layout, and UI mockups. Generate custom dummy text for your projects.",
    date: new Date("2026-05-15"),
    sections: [
      { heading: "What Is Lorem Ipsum?", body: "Lorem Ipsum is dummy text derived from a scrambled Latin passage by Cicero dating from 45 BC. It has been the standard placeholder text in the printing and typesetting industry since the 1500s. The text approximates the distribution of letters in English, creating a natural visual block of text for layout mockups." },
      { heading: "Why Use Placeholder Text?", body: "Placeholder text lets designers and developers focus on layout, typography, and visual hierarchy without being distracted by meaningful content. It reveals how much space text occupies, how headings and paragraphs relate visually, and whether the overall design achieves the desired balance." },
      { heading: "Lorem Ipsum Variations", body: "Classic Lorem Ipsum uses the standard passage. Variations include shortened versions (just the first few words), customized lengths (by paragraphs, sentences, or words), and themed generators that produce text in specific styles or topics." },
      { heading: "Using Placeholder Text in Web Design", body: "Use Lorem Ipsum when wireframing new pages, designing UI components, creating template layouts, and presenting mockups to clients. Real content should replace placeholder text before launch — search engines penalize pages with Latin filler text." },
      { heading: "Using the WebUtil Lorem Ipsum Generator", body: "Our free Lorem Ipsum Generator creates placeholder text in paragraphs, sentences, or words. Choose the amount of text you need and copy it instantly. All generation runs client-side in your browser." },
    ],
  },
  "jwt-authentication-explained": {
    title: "JWT Authentication Explained: How JSON Web Tokens Work",
    description: "A complete guide to JWT authentication — learn how JSON Web Tokens work, their structure, how they secure APIs, and best practices for implementation.",
    date: new Date("2026-06-01"),
    sections: [
      { heading: "What is JWT Authentication?", body: "JWT (JSON Web Token) authentication is a token-based authentication method where a server issues a signed JSON token that clients include in subsequent requests. The token contains encoded claims about the user (identity, permissions, expiration) and is digitally signed to prevent tampering. Unlike session-based auth, JWT is stateless — the server doesn't need to store session data, making it ideal for distributed systems and microservices architectures. JWTs are widely used in OAuth 2.0, OpenID Connect, and REST API authentication. Our JWT Decoder and JWT Expiration Checker tools help you inspect and debug your tokens during development." },
      { heading: "JWT Structure: Header, Payload, and Signature", body: "A JWT consists of three Base64URL-encoded parts separated by dots: Header, Payload, and Signature. The header typically contains the token type (JWT) and signing algorithm (HS256, RS256). The payload contains claims — registered claims (iss, sub, exp, iat, nbf), public claims, and private custom claims. The signature is computed by signing the header + payload with a secret key (HMAC) or private key (RSA/ECDSA). Use our JWT Decoder to inspect each part of your tokens." },
      { heading: "How JWT Works in API Authentication", body: "The authentication flow: 1) User logs in with credentials. 2) Server validates credentials and returns a JWT (access token) plus optional refresh token. 3) Client stores the token and includes it in the Authorization header as Bearer <token> for subsequent API requests. 4) Server verifies the token's signature and expiration on each request without querying a database. 5) When the access token expires, the client uses the refresh token to get a new one. This stateless flow scales horizontally because any server instance can verify any token independently." },
      { heading: "JWT Security Best Practices", body: "Always use HTTPS to prevent token interception. Set short expiration times (15-60 minutes) for access tokens. Use refresh tokens with longer lifetimes stored securely. Never store JWTs in localStorage — use httpOnly cookies for browser apps. Validate the signature, expiration (exp), not-before (nbf), issuer (iss), and audience (aud) on every request. Use RS256 (asymmetric) instead of HS256 (symmetric) in microservice environments so services can verify without knowing the signing key. Rotate signing keys regularly." },
      { heading: "Common JWT Pitfalls to Avoid", body: "Storing sensitive data in the payload (JWTs are encoded, not encrypted). Not validating the expiration claim, allowing expired tokens to be reused. Using a weak or leaked signing secret. Setting excessively long expiration times. Not implementing a token revocation strategy (JWT is stateless — revocation requires a blocklist). Missing the audience (aud) claim validation allows token reuse across different services. Forgetting that JWTs increase request size — keep claims minimal for performance." },
    ],
  },
  "json-vs-yaml-for-apis": {
    title: "JSON vs YAML for APIs: Choosing the Right Data Format",
    description: "Compare JSON and YAML for API development — learn when to use each format, their strengths and weaknesses, and best practices for API design.",
    date: new Date("2026-06-05"),
    sections: [
      { heading: "JSON vs YAML for APIs: Key Differences", body: "JSON (JavaScript Object Notation) and YAML (YAML Ain't Markup Language) are both data serialization formats used in API development, but they serve different primary purposes. JSON is the dominant format for REST and GraphQL API payloads because it is compact, natively parsable by browsers, and has strict syntax that prevents ambiguity. YAML excels in configuration files (Docker Compose, Kubernetes, CI/CD) where human readability is prioritized over machine efficiency. JSON uses braces and brackets with explicit syntax; YAML uses indentation with minimal punctuation. Our JSON Formatter and YAML Converter tools help you work with both formats interchangeably." },
      { heading: "When to Use JSON for APIs", body: "JSON should be your default choice for API request/response bodies. It is the standard for REST APIs, GraphQL queries, and webhook payloads. JSON's native support in JavaScript means no parsing overhead in browsers. Every major programming language has built-in JSON libraries. JSON is also the required format for many cloud services, serverless functions, and NoSQL databases (MongoDB, Firebase). If you're building a public API, JSON is the expected format. Our JSON Formatter helps you prepare and validate JSON payloads during API development." },
      { heading: "When to Use YAML for APIs", body: "YAML shines in API documentation (OpenAPI/Swagger specifications are YAML by default), configuration-driven APIs, and infrastructure-as-code. If your API is primarily consumed by developers writing configuration files (like deployment pipelines or internal tools), YAML provides better readability. YAML supports comments, which JSON does not — a significant advantage for configuration files that need inline documentation. Tools like Docker Compose, Kubernetes, Ansible, and GitHub Actions all use YAML. Our YAML Converter helps you convert between YAML and JSON when needed." },
      { heading: "Performance Comparison", body: "JSON parsing is generally 2-5x faster than YAML parsing because JSON has simpler syntax rules and no indentation-based structure. JSON payloads are typically 10-30% smaller than equivalent YAML. For high-throughput APIs serving millions of requests, JSON's parsing speed advantage matters. YAML's parser needs to track indentation levels, handle anchors/aliases, and support multiple document types, adding overhead. For low-traffic internal configuration APIs, YAML's readability may outweigh the performance cost." },
      { heading: "Converting Between JSON and YAML", body: "Conversion between JSON and YAML is straightforward because both represent hierarchical data structures. When converting JSON to YAML, objects become mappings, arrays become sequences, and values maintain their types. When converting YAML to JSON, mappings become objects, sequences become arrays, and YAML-specific features (comments, anchors, multi-document files) are lost. Our free YAML to JSON Converter handles both directions with live preview, making it easy to work across both formats." },
    ],
  },
  "uuid-v4-vs-uuid-v7": {
    title: "UUID v4 vs UUID v7: Choosing the Right UUID for Your Application",
    description: "Compare UUID v4 and UUID v7 — understand the differences between random and time-ordered UUIDs, database performance implications, and when to use each version.",
    date: new Date("2026-06-10"),
    sections: [
      { heading: "UUID v4 vs UUID v7: The Core Difference", body: "UUID v4 (RFC 4122) generates identifiers using 122 random bits, producing IDs that are uniformly distributed across the entire keyspace. UUID v7 (RFC 9562) embeds a Unix millisecond timestamp in the first 48 bits, with the remaining 74 bits being random. This seemingly small change has profound implications for database performance: UUID v4 causes random insertions into B-tree indexes (page splits, fragmentation), while UUID v7 appends new IDs sequentially, matching auto-increment performance. Our UUID Generator (v4) and UUID v7 Generator let you create and compare both formats." },
      { heading: "Database Performance: UUID v4 vs UUID v7", body: "UUID v4's random distribution causes each new row to be inserted at a random index position, forcing the B-tree to split pages and reorganize frequently. This increases write latency, bloats index size, and degrades cache locality. UUID v7's time-prefixed design appends new rows near the end of the index, reducing page splits by 80-90% and improving insert throughput by 2-5x on PostgreSQL and MySQL benchmarks. For tables with millions of rows, the performance gap widens significantly. UUID v7 also improves range queries — SELECT * FROM users WHERE id BETWEEN ? AND ? benefits from sequential storage." },
      { heading: "When to Use UUID v4", body: "UUID v4 remains the better choice when you need unpredictability — for example, generating opaque public identifiers for API resources, creating session tokens, or preventing sequential ID enumeration by users. UUID v4 is also the most widely supported UUID version across languages, libraries, and databases. If you have an existing system using UUID v4, the migration cost to v7 often outweighs the performance benefit for small-to-medium databases (under 10 million rows). For distributed tracing and event sourcing where ID order is irrelevant, v4's simplicity wins." },
      { heading: "When to Use UUID v7", body: "UUID v7 is ideal for primary keys in OLTP databases, time-ordered event streams, chat message IDs, financial transaction IDs, time-series data, and any use case where insert performance and B-tree index health matter. PostgreSQL users benefit most — CockroachDB has adopted UUID v7 as its default key encoding. If you're starting a new project with a relational database, UUID v7 should be your default choice for primary keys. The time component also enables chronological sortability without a separate created_at column." },
      { heading: "UUID v7 Implementation and Support", body: "UUID v7 support is growing rapidly. Python 3.14+ includes uuid.uuid7() in the standard library. Go's google/uuid package supports v7 via uuid.NewV7(). Node.js needs a polyfill (uuidv7 npm package). PostgreSQL doesn't natively generate UUID v7 but can use extensions or application-layer generation. MySQL 8.0+ accepts UUID v7 values in BINARY(16) columns. Most UUID libraries still default to v4, so you may need to explicitly request v7. Our UUID v7 Generator provides client-side v7 generation that works in any browser." },
    ],
  },
  "regex-tutorial": {
    title: "Regex Tutorial for Beginners: Learn Regular Expressions with Examples",
    description: "A complete regex tutorial for beginners. Learn how to use regular expressions for pattern matching, text search, and data validation with practical examples.",
    date: new Date("2026-06-14"),
    sections: [
      { heading: "What is a Regular Expression?", body: "A regular expression (regex) is a sequence of characters that defines a search pattern. Regex is used for pattern matching in strings — finding, replacing, and validating text. Every programming language supports regex in some form: JavaScript has RegExp, Python has re module, Go has regexp package, and bash has grep/sed/awk. Regex syntax is standardized across languages with minor differences. Our Regex Tester lets you experiment with patterns in real-time." },
      { heading: "Regex Basic Syntax: Characters and Anchors", body: "Literal characters match themselves: /hello/ matches 'hello' in any string. The dot . matches any single character except newline. Anchors ^ and $ match the start and end of a string (or line with multiline flag). Word boundary \\b matches between a word character and non-word character. Examples: /^hello/ matches 'hello' only at the start, /world$/ matches 'world' only at the end, /\\bword\\b/ matches the exact word 'word' but not 'sword' or 'words'." },
      { heading: "Character Classes and Quantifiers", body: "Character classes let you match sets of characters: [aeiou] matches any vowel, [a-z] matches any lowercase letter, [0-9] matches any digit. Negated classes use ^ inside brackets: [^0-9] matches any non-digit. Predefined classes include \\d (digit), \\w (word char), \\s (whitespace). Quantifiers control repetition: * (zero or more), + (one or more), ? (zero or one), {3} (exactly 3), {2,4} (2 to 4). Example: /\\d{3}-\\d{4}/ matches phone-like patterns like '555-1234'." },
      { heading: "Groups, Capturing, and Alternation", body: "Parentheses () create capturing groups that extract matched substrings: /(\\w+)@(\\w+)\\.(\\w+)/ captures email parts. Non-capturing groups (?:...) group without capturing. Alternation | matches one of several patterns: /cat|dog|bird/ matches any of the three words. Backreferences like \\1 reference captured groups within the pattern. Named groups (?<name>...) make patterns self-documenting. Example: /(?<year>\\d{4})-(?<month>\\d{2})-(?<day>\\d{2})/ extracts date components." },
      { heading: "Practical Regex Examples and Use Cases", body: "Email validation: /^[\\w.-]+@[\\w.-]+\\.\\w{2,}$/. URL matching: /https?:\\/\\/[\\w.-]+(?:\\/[\\w.-]*)*\\/?/. Phone numbers: /^\\+?1?[\\s.-]?\\(?\\d{3}\\)?[\\s.-]?\\d{3}[\\s.-]?\\d{4}$/. Password strength: /^(?=.*[a-z])(?=.*[A-Z])(?=.*\\d)(?=.*[^\\w\\s]).{8,}$/ requires uppercase, lowercase, digit, and special character. IP addresses: /^(?:\\d{1,3}\\.){3}\\d{1,3}$/. Use our Regex Tester to build and test your patterns interactively." },
    ],
  },
  "css-units-guide": {
    title: "CSS Units Guide: px, rem, em, vh, vw, and More Explained",
    description: "A complete guide to CSS units. Learn the difference between px, rem, em, vh, vw, and other CSS units. Understand when to use each unit for responsive web design.",
    date: new Date("2026-06-14"),
    sections: [
      { heading: "Why CSS Units Matter", body: "CSS units determine how elements are sized and positioned on the web. Choosing the right unit affects responsiveness, accessibility, and maintainability. Absolute units (px, cm, in) have fixed sizes. Relative units (rem, em, vh, vw, %) adapt to the viewport, parent element, or root font size. Modern responsive design relies primarily on relative units so layouts work across devices of all sizes. Use our px to rem Converter to translate between absolute and relative units easily." },
      { heading: "px: The Pixel Unit", body: "The px (pixel) unit is the most common absolute unit in CSS. One pixel equals one dot on the screen. Pixels are reliable for precise layouts like borders (1px solid), box shadows, and fine details. However, px units do not scale when users change their browser's default font size for accessibility. This makes px problematic for font sizes and spacing in accessible design. Use px for visual details that must stay sharp — borders, outlines, and media queries for specific breakpoints." },
      { heading: "rem and em: Relative Font Units", body: "rem (root em) is relative to the root element's font size (default 16px in browsers). 1rem = 16px, 2rem = 32px. rem units respect user accessibility settings — if a user increases their base font size, everything scales proportionally. em is relative to the parent element's font size, which can compound (a 2em child inside a 2em parent is 4x the base). Use rem for most sizing (fonts, spacing, padding, margins) and em sparingly for components that should scale relative to their own font size, like buttons with padding that grows with text size. Our px to rem Converter helps you transition from px to rem." },
      { heading: "vw, vh, vmin, vmax: Viewport Units", body: "Viewport units are relative to the browser viewport: vw (1% of viewport width), vh (1% of viewport height), vmin (1% of the smaller dimension), vmax (1% of the larger dimension). Use 100vw for full-width elements, 100vh for full-height hero sections, and vmin/vmax for responsive font sizes that adapt to both portrait and landscape orientations. Caution: 100vw includes scrollbar width, which may cause horizontal overflow. Combine vw with clamp() for fluid typography: font-size: clamp(1rem, 2.5vw, 3rem) creates text that scales smoothly between min and max sizes." },
      { heading: "Percentage, ch, and Other CSS Units", body: "Percentage (%) is relative to the parent element's same property — width: 50% takes half the parent's width. The ch unit equals the width of the '0' character, useful for setting max-width on text containers (max-width: 60ch improves readability). The ex unit equals the x-height of the font. The fr unit in CSS Grid distributes available space (grid-template-columns: 1fr 2fr gives the second column twice the space). For modern responsive design, use rem for sizing and spacing, vw for full-width layouts, % for flexible layouts, ch for text containers, and px only for borders and fine details. Our CSS px to rem Converter helps you migrate legacy px values to rem." },
    ],
  },
  "docker-compose-tutorial": {
    title: "Docker Compose Tutorial for Beginners: Define and Run Multi-Container Apps",
    description: "Learn Docker Compose from scratch. Define multi-container applications, manage services, networks, and volumes with docker-compose.yml files.",
    date: new Date("2026-06-14"),
    sections: [
      { heading: "What is Docker Compose?", body: "Docker Compose is a tool for defining and running multi-container Docker applications. With a single docker-compose.yml file, you define your application's services, networks, and volumes. One command — docker compose up — starts everything. Compose eliminates the need to memorize complex docker run commands and ensures consistent environments across development, testing, and production. Use our Docker Compose Validator to check your YAML syntax before deploying." },
      { heading: "Docker Compose File Structure", body: "A docker-compose.yml file starts with version (optional in modern Compose), then defines services, networks, and volumes. The services section is required — each service defines a container with an image (from Docker Hub) or build context (local Dockerfile). Key service fields: image (nginx:alpine), build (./app), ports (80:80), environment (NODE_ENV=production), volumes (./data:/data), depends_on (db), and restart (always). Networks connect services, volumes persist data. Example: a basic LAMP stack defines web (Apache), db (MySQL), and php (PHP-FPM) services with appropriate connections." },
      { heading: "Common Docker Compose Commands", body: "docker compose up -d starts all services in detached mode. docker compose down stops and removes containers, networks, and default volumes. docker compose logs -f follows all service logs. docker compose ps shows running containers. docker compose exec service_name bash opens a shell inside a running container. docker compose build rebuilds service images. docker compose pull fetches latest images. docker compose restart service_name restarts a specific service. docker compose config validates your YAML and shows the resolved configuration — use our Docker Compose Validator for quick client-side checks." },
      { heading: "Docker Compose for Development vs Production", body: "For development, Compose provides hot-reloading by mounting code as volumes (./app:/app), using environment files (.env), and exposing debug ports. Override files (docker-compose.override.yml) add development-specific config without modifying the base file. For production, use docker compose -f docker-compose.yml -f docker-compose.prod.yml to layer configurations. Production adjustments include removing volume mounts for code (use image builds instead), adding restart policies, configuring resource limits, using named volumes, and setting up health checks. Always validate your production config with our Docker Compose Validator before deployment." },
      { heading: "Docker Compose Best Practices", body: "Use specific image tags (not :latest) to ensure reproducible builds. Keep services small and focused — one process per container. Use depends_on with condition: service_healthy to control startup order. Set resource limits with deploy.resources for production. Use .env files for environment-specific variables. Define custom networks instead of relying on the default bridge network. Use named volumes for persistent data (databases) and bind mounts for development. Validate your YAML files with our Docker Compose Validator before every deployment to catch syntax errors early." },
    ],
  },
  "rest-api-design": {
    title: "REST API Design Best Practices: Build Better Web APIs in 2026",
    description: "Learn REST API design best practices including resource naming, HTTP methods, status codes, pagination, error handling, and versioning strategies.",
    date: new Date("2026-06-14"),
    sections: [
      { heading: "What Makes a Good REST API?", body: "A well-designed REST API is intuitive, consistent, and predictable. Good API design reduces development time, minimizes integration errors, and creates a developer experience that people enjoy working with. Key principles: resource-oriented URLs, proper HTTP method usage, meaningful status codes, consistent error formats, and comprehensive documentation. Whether you're building a public API or internal microservice, following REST conventions makes your API easier to adopt. Use our JSON Formatter to inspect and debug API responses during development." },
      { heading: "Resource Naming Conventions", body: "Use nouns for resources, not verbs: /users instead of /getUsers. Use plural nouns for collections: /users, /orders, /products. Use nested routes for relationships: /users/123/orders, /orders/456/items. Keep URLs lowercase with hyphens: /order-items not /orderItems. Use query parameters for filtering, sorting, and pagination: /users?role=admin&sort=created_at&order=desc. Avoid deep nesting — limit to two levels, then use query parameters or dedicated endpoints. Consistent naming is the foundation of a usable API." },
      { heading: "HTTP Methods and Status Codes", body: "GET for retrieval (200 OK), POST for creation (201 Created), PUT for full replacement (200 OK), PATCH for partial updates (200 OK), DELETE for removal (204 No Content). Common status codes: 400 Bad Request (invalid input), 401 Unauthorized (missing auth), 403 Forbidden (insufficient permissions), 404 Not Found (resource doesn't exist), 409 Conflict (duplicate resource), 422 Unprocessable Entity (validation failed), 429 Too Many Requests (rate limited), 500 Internal Server Error (server fault). Always return the most specific status code — it helps client developers debug integration issues." },
      { heading: "Error Handling and Pagination", body: "Standardized error responses: return a consistent JSON structure with error code, message, and optional details. Example: { error: 'validation_error', message: 'Email is required', details: [{ field: 'email', issue: 'required' }] }. For pagination, support cursor-based pagination for large datasets (cursor-based is more reliable than offset-based when data changes). Response format: { data: [...], pagination: { cursor: 'abc123', has_more: true, total: 1000 } }. Include rate limiting headers (X-RateLimit-Remaining, X-RateLimit-Reset) so clients can self-regulate. Our JSON Formatter helps you prettify API responses during development." },
      { heading: "API Versioning and Documentation", body: "Version your API from day one. Use URL-based versioning (/v1/users, /v2/users) for simplicity, or accept-header versioning (Accept: application/vnd.api+json;version=2) for cleaner URLs. Document versions clearly and maintain migration guides. Use OpenAPI/Swagger (YAML format) for API documentation — it generates interactive docs, client SDKs, and server stubs automatically. Include request/response examples, authentication requirements, and error codes. Deprecate old versions with clear timelines and Sunset HTTP headers. Our URL Encoder tool helps you properly encode query parameters and API URLs." },
    ],
  },
  "how-qr-codes-work": {
    title: "How QR Codes Work: A Complete Guide to QR Code Technology",
    description: "Learn how QR codes work — from error correction and data encoding to structure and generation. Understand the technology behind every QR code you scan.",
    date: new Date("2026-06-14"),
    sections: [
      { heading: "What Is a QR Code?", body: "A QR Code (Quick Response Code) is a two-dimensional barcode invented by Denso Wave in 1994. Unlike traditional barcodes that store data in one dimension (width of bars), QR codes store data in two dimensions using black and white squares arranged in a grid. This allows QR codes to hold significantly more data — up to 3KB versus 20-25 bytes for a standard barcode. QR codes can encode numeric data, alphanumeric characters, bytes/binary data, and Japanese Kanji/Kana characters. Use our QR Code Generator to create your own codes instantly in your browser." },
      { heading: "QR Code Structure and Components", body: "A QR code has several structural elements: the finder patterns (three large squares in corners) let scanners detect position and orientation. Timing patterns (alternating black/white lines) help the scanner determine module size. Alignment patterns (smaller squares) aid scanning on curved or angled surfaces. The format information encodes error correction level and mask pattern. The data area stores the actual content. The quiet zone (white border) separates the QR code from surrounding text. Understanding this structure helps when printing QR codes — never crop the quiet zone or alter the finder patterns." },
      { heading: "Error Correction in QR Codes", body: "QR codes use Reed-Solomon error correction, allowing them to be read even when partially damaged or obscured. Four levels: L (Low, 7% recovery), M (Medium, 15%), Q (Quartile, 25%), H (High, 30%). Higher error correction means more redundant data is stored, resulting in a denser QR code. For maximum scan reliability, use level H — logos and designs can cover up to 30% of the code while remaining scannable. For simple black-and-white QR codes on stable surfaces (business cards, posters), level M provides good balance between density and durability. Our QR Code Generator creates codes optimized for reliable scanning." },
      { heading: "QR Code Data Capacity and Encoding Modes", body: "QR codes support four encoding modes: numeric (up to 7,089 characters), alphanumeric (up to 4,296 characters — includes digits, uppercase letters, and symbols), byte/binary (up to 2,953 bytes — supports UTF-8 text and URLs), and Kanji/Kana (up to 1,817 characters for Japanese text). QR codes automatically select the most efficient encoding mode based on the data. Version determines the grid size — version 1 is 21x21 modules, version 40 is 177x177 modules. Higher versions hold more data but require higher printing resolution. For most URLs, version 2-4 (25x25 to 33x33) is sufficient." },
      { heading: "QR Code Use Cases and Best Practices", body: "Common use cases: URLs (link to websites, app stores), vCard contacts (scan to save to phone), Wi-Fi credentials (scan to connect), payment links (scan to pay), event check-in (tickets with QR codes), and product packaging (scan for info). Best practices: always test your QR code with multiple devices and apps. Ensure minimum print size of 2cm x 2cm (0.8 inches) for reliable scanning. Maintain high contrast between modules and background. Test with your QR Code Generator before mass printing — verify the data is correct and all URLs use HTTPS for security." },
    ],
  },
  "common-jwt-errors": {
    title: "Common JWT Errors and How to Debug Them",
    description: "A practical guide to debugging JWT parsing errors — base64 decoding issues, Bearer prefix problems, signature mismatches, and how to fix them.",
    date: new Date("2026-06-28"),
    sections: [
      { heading: "Why JWT Parsing Fails", body: "JWT parsing errors are some of the most frustrating issues in API development. The error messages are often cryptic — 'illegal base64 data at input byte 0', 'signature is invalid', or 'token contains an invalid number of segments'. Most of these errors have simple causes: including the Bearer prefix in the token string, using the wrong signing key, or passing an expired token. Understanding what each error means saves hours of debugging. Use our JWT Decoder to inspect token parts and spot issues instantly." },
      { heading: "The Bearer Prefix Problem", body: "The most common JWT error is passing 'Bearer <token>' instead of just '<token>' to the parser. When you extract the Authorization header, it contains 'Bearer eyJhbGci...'. If you pass this whole string to jwt.ParseWithClaims (Go), jwt.verify (Node.js), or any parser, the library tries to base64-decode 'Bearer ' as the header segment, which fails with 'illegal base64 data at input byte 0'. Always strip the prefix: tokenString = strings.TrimPrefix(tokenString, 'Bearer ') in Go, or token = authHeader.replace('Bearer ', '') in JavaScript. Our JWT Decoder accepts tokens with or without the prefix and shows you the decoded parts." },
      { heading: "Signature Validation Errors", body: "'Signature is invalid' or 'token signature does not match' means the token was signed with a different secret or key than what you're using to verify. Common causes: using HS256 (symmetric) signing but passing an RSA public key for verification, rotating the signing secret without accounting for tokens issued under the old secret, or environment mismatch (development vs production secret). For Go's jwt.ParseWithClaims, check that your token's signing method matches the expected method in the callback. The error 'unexpected signing method' fires when the token uses a different algorithm than your code expects. Always verify the algorithm in your parser: if _, ok := token.Method.(*jwt.SigningMethodHMAC); !ok { return nil, errors.New('unexpected signing method') }." },
      { heading: "Expiration and Claim Validation", body: "'Token is expired' is straightforward — the exp (expiration) claim has passed. But 'invalid claims' or structural errors often mean missing required claims. If your parser expects custom claims (like a UserClaims struct), but the token only contains standard registered claims (iss, sub, exp, iat), parsing fails. Always log the raw error from the parser. For Go: _, err := jwt.ParseWithClaims(...) — the err contains the specific failure reason. Validate all standard claims: iss (issuer) should match your auth server URL, aud (audience) should match your service name, exp must be in the future. Our JWT Expiration Checker decodes and displays all claims so you can verify expiration and payload structure at a glance." },
      { heading: "Multi-Secret Key Rotation Strategy", body: "When rotating JWT signing keys, tokens issued under the old key will fail validation after rotation. The solution: maintain a list of valid keys and try each one. In Go, collect all valid secrets in a slice and attempt parsing with each. In the jwt.ParseWithClaims key function, iterate over keys and return the first one that matches the token's key ID (kid header). Include a grace period where both old and new keys are accepted. Store the kid in the JWT header so your parser can select the correct key without trying all possibilities. Use our JWT Decoder to inspect the kid header and verify which key was used to sign. This pattern lets you rotate keys without invalidating existing sessions." },
    ],
  },
  "json-compact-pretty-print": {
    title: "JSON Compact Pretty-Print: Smart Formatting Between Minified and Expanded",
    description: "Learn how smart JSON formatting works — keeping small objects on one line while expanding large ones. A practical guide to compact pretty-print techniques.",
    date: new Date("2026-06-30"),
    sections: [
      { heading: "What Is Compact Pretty-Print?", body: "Standard JSON formatters offer two modes: minified (everything on one line) or pretty-printed (everything expanded with indentation). Compact pretty-print (also called smart formatting) is a middle ground — small objects and arrays stay on one line while large structures are expanded. This preserves readability for shallow data while keeping deep structures navigable.\n\nPython's json module doesn't support this natively, but you can achieve it with a custom encoder or by using tools that offer format/compact toggle options like our JSON Formatter. The key insight is that JSON readability is not binary — different data shapes benefit from different formatting strategies. A configuration object with three keys is perfectly readable on one line, while a list of 100 user records needs vertical space.\n\nCompact formatting solves the problem that developers face daily: API responses that mix small metadata with large datasets. When you curl an endpoint and get back a wall of text, you toggle to pretty-print and suddenly a 10-line response becomes 300 lines. Compact formatting prevents this by being smart about what to expand and what to keep inline." },
      { heading: "When to Use Compact Formatting", body: "Compact formatting is ideal for log output where you need to see structure quickly without excessive line count. Development API responses become significantly more readable when small config objects stay inline but large data arrays are expanded for browsing. Configuration files benefit from compact formatting because it reduces vertical scrolling while preserving the structural understanding.\n\nDebug output with mixed-depth data is where compact formatting truly shines. Consider a response containing a small metadata object alongside a large items array. In standard pretty-print, the metadata takes 15 lines for 3 keys. In compact mode, it takes 1 line. This difference compounds across hundreds of API calls during a debugging session.\n\nOur JSON Formatter lets you toggle between expanded, compact, and minified views with one click. This means you can choose the right view for your task without switching tools. Compact mode is particularly useful when you need to share JSON snippets in documentation or chat messages where space is limited." },
      { heading: "Implementing Compact Pretty-Print in Python", body: "To implement compact formatting in Python, subclass json.JSONEncoder and override the encode method. Set a max_inline_length threshold (e.g., 80 characters) to determine what qualifies as compact. Use regex to detect small objects and arrays that can safely be inlined.\n\nHere is the core approach: first serialize your data with standard pretty-print (indent=2). Then post-process the output with regular expressions. For objects: re.sub(r'\\{([^}]+)\\}', lambda m: '{' + m.group(1).strip() + '}' if len(m.group(0)) <= threshold else m.group(0), s). For arrays: re.sub(r'\\[([^\\]]+)\\]', lambda m: '[' + m.group(1).strip() + ']' if len(m.group(0)) <= threshold else m.group(0), s).\n\nThe threshold determines the behavior: lower values produce more expanded output, higher values produce more compact output. A threshold of 80 characters works well for terminal-width output. For code documentation, you might want a tighter threshold of 60 characters to avoid horizontal scrolling. This approach requires no external dependencies and gives you fine-grained control over the formatting behavior." },
      { heading: "Compact Formatting in Other Languages", body: "JavaScript developers can achieve compact formatting by using JSON.stringify with a replacer function and post-processing the output with regex, similar to the Python approach. Node.js has the `json-stringify-pretty-compact` npm package that provides this behavior out of the box with customizable options including maxLength, indent, and arrayProcessor.\n\nGo's encoding/json package doesn't support compact mode natively, but you can implement custom MarshalJSON methods on your structs. The approach is to marshal the struct normally, then run a post-processing step that inlines short values. Alternatively, you can use the jsonparser or ffjson libraries that give you more control over serialization.\n\nRust's serde_json library has both PrettyFormatter and CompactFormatter. You can write a custom formatter that switches between them based on the depth or size of the current value. This requires implementing the serde_json::ser::Formatter trait, which gives you hooks at every stage of serialization.\n\nFor developers who need compact formatting occasionally without writing code, our JSON Formatter is the fastest path. It supports all three modes — expanded, compact, and minified — with instant switching and no setup required." },
      { heading: "Comparing JSON Formatting Modes", body: "The three formatting modes serve different purposes and understanding their tradeoffs helps you choose the right one for each task. Minified mode removes all whitespace, producing the smallest possible output. This is ideal for API responses where bandwidth matters, for storing JSON in databases, and for machine-to-machine communication. A typical API response with 100 items is about 3KB minified.\n\nPretty-printed mode adds full indentation with each level getting its own line. This is the most readable format for humans browsing data structures. It makes nested relationships visually obvious and simplifies debugging. The same 100-item response is about 6KB pretty-printed — double the size of minified but far more readable.\n\nCompact mode strikes the balance between the two. Structure is indented so you can see the hierarchy, but leaf nodes — small objects and arrays — stay on one line. For the same 100-item response, compact mode produces about 4KB. This is ideal for development workflows because it is readable enough to inspect at a glance while being compact enough to fit in terminal output and log files.\n\nTry all three modes on our JSON Formatter to see which works best for your specific data. The right choice depends on your context: use minified for production APIs, pretty-printed for deep debugging, and compact for daily development work." },
    ],
  },
  "json-to-csv-guide": {
    title: "JSON to CSV Conversion: The Complete Developer's Guide",
    description: "Learn how to convert JSON to CSV in JavaScript, Python, and online tools. Handle nested objects, arrays, and large datasets efficiently.",
    date: new Date("2026-07-01"),
    sections: [
      { heading: "Why Convert JSON to CSV?", body: "JSON and CSV serve different purposes in the data ecosystem. JSON excels at representing hierarchical data with nested objects and arrays, making it ideal for APIs and configuration files. CSV, on the other hand, is the universal format for spreadsheets, data analysis tools, and database imports. Converting between them is a common task for developers working with data pipelines.\n\nThe most frequent use cases for JSON to CSV conversion include exporting API data to Excel or Google Sheets for business analysis, preparing data for machine learning training sets, migrating data between systems that use different formats, and generating reports from JSON log files. CSV's simplicity makes it the preferred format for non-technical stakeholders who need to work with data in spreadsheet applications.\n\nOur JSON to CSV Converter handles this conversion automatically with support for nested objects, custom delimiters, and BOM for Excel compatibility. The tool runs entirely in your browser, ensuring sensitive data never leaves your machine." },
      { heading: "JSON to CSV Conversion Rules", body: "Converting JSON to CSV requires flattening hierarchical data into a tabular structure. The basic rule is that each JSON object becomes one row in the CSV, and each key becomes a column header. Simple flat JSON objects convert directly — keys become column headers, values become cells. The order of columns is determined by the first object in the array, with additional keys from subsequent objects appended as new columns.\n\nNested objects present a challenge because CSV cannot represent hierarchy. The standard approach is to flatten nested keys using dot notation: a nested object like {\"user\": {\"name\": \"Alice\", \"email\": \"alice@example.com\"}} becomes columns user.name and user.email. This preserves the structural information while fitting into the tabular format. Our tool automatically flattens nested objects this way.\n\nArrays within JSON objects are more complex. Simple arrays of primitive values (strings, numbers) can be joined into a single cell with a separator. Arrays of objects require different handling — each object in the array can create additional columns (user.addresses[0].street, user.addresses[1].street) or the array can be serialized as a JSON string within the cell. Our JSON to CSV Converter offers configurable array handling to match your needs." },
      { heading: "JSON to CSV in JavaScript", body: "JavaScript provides several approaches for JSON to CSV conversion, from manual implementation to library-based solutions. For simple flat JSON, you can extract keys from the first object, map each object to a CSV row, and join everything with newlines. The core logic is straightforward: const headers = Object.keys(data[0]); const rows = data.map(obj => headers.map(h => JSON.stringify(obj[h] ?? '')).join(',')); const csv = [headers.join(','), ...rows].join('\\n').\n\nFor production use, the PapaParse library is the most popular choice for CSV parsing and generation in JavaScript. It handles edge cases like values containing commas, line breaks within cells, and proper escaping. For Node.js environments, the csv-stringify package from the csv project provides stream-based CSV generation that handles large datasets without loading everything into memory.\n\nBrowser-based conversion has the advantage of keeping data local. Our JSON to CSV Converter uses the File API and Blob downloads to generate CSV files without any server upload. This makes it suitable for sensitive data that cannot be sent to external services. The conversion happens entirely in JavaScript, using the techniques described above, with optimizations for handling arrays of hundreds of thousands of rows." },
      { heading: "JSON to CSV in Python", body: "Python offers excellent libraries for JSON to CSV conversion through the standard library and pandas. The simplest approach uses the built-in json and csv modules: load JSON with json.loads(), open a CSV file with csv.writer(), write the header row from the keys of the first object, then iterate and write each row. For flat JSON, this is about 10 lines of code and requires no external dependencies.\n\nFor complex JSON with nested structures, pandas provides the most convenient path. Use pd.json_normalize() to flatten nested JSON into a DataFrame, then df.to_csv() to export. The json_normalize function handles nested objects with dot notation, nested arrays with record_path, and custom separators. It also deals with missing values gracefully by filling with NaN or a specified fill value.\n\nFor very large JSON files that don't fit in memory, use ijson for streaming JSON parsing combined with csv streaming writer. This processes the JSON incrementally without loading the entire file into memory, making it suitable for multi-gigabyte datasets. The tradeoff is more complex code — you need to know the structure of your JSON in advance to define the streaming paths. Our online JSON to CSV Converter handles files up to 50MB in the browser using streaming chunked processing." },
      { heading: "Using WebUtil's JSON to CSV Converter", body: "Our free JSON to CSV Converter provides a simple interface for converting JSON data into CSV format. Paste your JSON array, configure the conversion options, and download the result as a CSV file. The tool supports several options to handle different JSON structures and output requirements.\n\nKey features include BOM insertion for Excel compatibility — Excel often misinterprets UTF-8 CSV files without a Byte Order Mark, especially when they contain non-ASCII characters. The tool also supports custom delimiter selection (comma, semicolon, tab, pipe) for regional Excel variants that expect semicolons. Nested object flattening is automatic using dot notation, and you can choose how arrays are handled.\n\nAll processing runs client-side using your browser's JavaScript engine. No data is uploaded to any server — your JSON stays on your machine. This is critical for developers working with sensitive data like customer records, financial data, or proprietary information. The converter handles arrays of up to 50,000 rows efficiently, with larger datasets processed in chunks to keep the browser responsive." },
    ],
  },
  "kubernetes-yaml-guide": {
    title: "Kubernetes YAML: Writing Production-Grade Manifests",
    description: "A complete guide to writing Kubernetes YAML manifests — Pods, Deployments, Services, ConfigMaps, and validation best practices.",
    date: new Date("2026-07-02"),
    sections: [
      { heading: "What Is Kubernetes YAML?", body: "Kubernetes uses YAML files to define the desired state of your cluster resources. Every object in Kubernetes — Pods, Deployments, Services, ConfigMaps, Secrets, Ingresses — is created from a YAML manifest that describes what you want and how it should run. Understanding Kubernetes YAML is essential for anyone working with container orchestration.\n\nA Kubernetes YAML manifest follows a consistent structure across all resource types. Every manifest has four required top-level fields: apiVersion (which API version to use), kind (the resource type), metadata (name, namespace, labels, annotations), and spec (the desired state configuration). This consistency means once you understand one resource type, learning others is a matter of learning their specific spec fields.\n\nThe challenge most developers face is getting the YAML syntax correct. Kubernetes is strict about indentation — an extra space can cause your entire manifest to be rejected. Common mistakes include using tabs instead of spaces, incorrect nesting of fields, and missing required spec properties. Our Kubernetes YAML Validator helps catch these errors before you apply manifests to your cluster, saving time and preventing accidental misconfiguration." },
      { heading: "Writing Pod and Deployment Manifests", body: "Pods are the smallest deployable units in Kubernetes, representing one or more containers that share storage and network resources. A simple Pod manifest includes containers with their image, ports, environment variables, and resource requests. Pods are typically not created directly in production — you use higher-level resources like Deployments to manage them.\n\nDeployments are the standard way to run stateless applications in Kubernetes. A Deployment manages a ReplicaSet, which in turn manages the Pods. Key spec fields include replicas (how many Pod instances to run), selector (which Pods to manage via labels), and template (the Pod template defining containers, volumes, and other settings). Deployments support rolling updates with configurable strategies like maxSurge and maxUnavailable for zero-downtime deployments.\n\nProduction-grade Deployments should include resource requests and limits for CPU and memory, liveness and readiness probes for health checking, pod disruption budgets for maintenance windows, and horizontal pod autoscaling for dynamic scaling. These configuration details transform a basic deployment into a resilient production service. Our Kubernetes YAML Validator checks for these best practices and flags missing configurations." },
      { heading: "Services, ConfigMaps, and Secrets", body: "Services provide stable networking for Pods, which are ephemeral and get new IP addresses when they restart. The most common Service types are ClusterIP (internal cluster access, default), NodePort (external access via node port), and LoadBalancer (cloud provider load balancer). Services use label selectors to determine which Pods receive traffic, decoupling the network endpoint from the actual Pod instances.\n\nConfigMaps and Secrets store configuration data separately from container images. ConfigMaps hold non-sensitive configuration in key-value pairs or structured data like YAML files. They can be exposed to Pods as environment variables, command-line arguments, or mounted as volumes. The advantage is that you can change configuration without rebuilding container images.\n\nSecrets work like ConfigMaps but are intended for sensitive data like passwords, API keys, and certificates. Values in Secrets are base64-encoded (not encrypted — encryption at rest requires additional configuration like KMS). For production environments, consider external secret management tools like HashiCorp Vault, AWS Secrets Manager, or Sealed Secrets for GitOps workflows." },
      { heading: "Common Kubernetes YAML Errors", body: "The most frequent errors in Kubernetes YAML manifests are indentation mistakes. YAML uses spaces for indentation, and Kubernetes is strict about the exact nesting structure required by the API spec. A common example is placing spec fields at the wrong level, like putting containers directly under spec instead of under spec.template.spec. These errors produce cryptic messages from kubectl apply that can be hard to debug.\n\nAPI version mismatches are another common issue. Kubernetes deprecates API versions across releases — for example, apps/v1beta1 was removed in Kubernetes 1.16. Using a deprecated API version causes apply failures. Always check the Kubernetes API versioning documentation for your cluster version. The kubectl explain command shows the correct API version and field structure for any resource.\n\nMissing required fields is the third category of common errors. Every resource has required fields that must be present. A Deployment requires spec.selector.matchLabels and spec.template.metadata.labels to overlap. An Ingress requires spec.rules. Our Kubernetes YAML Validator checks for these structural requirements and provides clear error messages to help you fix issues before deployment." },
      { heading: "YAML Validation Best Practices", body: "Validating Kubernetes YAML before applying it to a cluster is a critical part of any deployment pipeline. Manual validation steps include using kubectl apply --dry-run=client to check syntax locally, kubectl explain to verify field structures, and schema validation tools to check against the OpenAPI spec that Kubernetes publishes for each version.\n\nFor CI/CD pipelines, integrate validation at multiple stages. Run syntax checks in pre-commit hooks, schema validation in pull request checks, and dry-run applies against a staging cluster before production. Tools like kubeconform, kubeval, and our Kubernetes YAML Validator can be integrated into GitHub Actions, GitLab CI, or any pipeline that runs containerized jobs.\n\nOur free Kubernetes YAML Validator provides instant client-side validation of your manifests. It checks YAML syntax, Kubernetes schema compliance, and common configuration issues. Paste your manifest and get immediate feedback without needing a cluster connection. All validation runs in your browser — your manifests never leave your machine, making it safe for confidential configurations." },
    ],
  },
  "github-actions-guide": {
    title: "GitHub Actions CI/CD: From Zero to Production Pipeline",
    description: "Learn GitHub Actions from scratch — workflow syntax, build/test/deploy pipelines, matrix builds, and YAML validation.",
    date: new Date("2026-07-03"),
    sections: [
      { heading: "What Are GitHub Actions?", body: "GitHub Actions is GitHub's built-in CI/CD platform that automates software workflows directly from your repository. You can build, test, and deploy your code right from GitHub with workflows triggered by events like pushes, pull requests, issue creation, or scheduled intervals. Actions supports all major languages and platforms with a marketplace of over 10,000 pre-built actions.\n\nThe core concept is a workflow — a YAML file in the .github/workflows directory that defines a series of jobs to run when triggered. Each job runs in a fresh runner environment (Ubuntu, Windows, or macOS) and consists of steps that can run commands or use actions from the marketplace. Workflows can be as simple as running tests on every push or as complex as multi-environment deployment pipelines with approval gates.\n\nGitHub Actions uses YAML syntax with specific keywords for workflow configuration. The most important keywords are name, on (trigger events), jobs (parallel units of work), steps within each job, and actions within steps. Understanding these building blocks lets you construct any CI/CD pipeline you need. Our GitHub Actions Validator helps you check your workflow syntax before committing." },
      { heading: "Workflow Syntax and Structure", body: "A GitHub Actions workflow file starts with the workflow name and trigger events. The on keyword specifies what events start the workflow — push, pull_request, schedule, workflow_dispatch (manual trigger), or repository_dispatch (external trigger). You can filter triggers by branch, path, or tags using the branches, paths, and tags keywords under each event.\n\nThe jobs section defines what work to perform. Jobs run in parallel by default but you can create dependencies with the needs keyword: job2: needs: job1 ensures job2 waits for job1 to complete. Each job specifies a runs-on (runner type), optional services (Docker containers for dependencies like databases), and a sequence of steps that execute commands or actions.\n\nSteps are the smallest unit of work. A step uses either the run keyword (to execute shell commands) or the uses keyword (to invoke a marketplace action). Common actions include actions/checkout (check out your repository), actions/setup-node (install Node.js), and actions/upload-artifact (save build outputs). Steps can access environment variables and previous step outputs through the steps context. Our GitHub Actions Validator checks your YAML syntax and catches common structural errors." },
      { heading: "Building a Complete CI/CD Pipeline", body: "A production CI/CD pipeline typically has four stages: lint, test, build, and deploy. The lint stage runs code quality tools like ESLint, Prettier, or RuboCop. The test stage runs unit tests, integration tests, and possibly end-to-end tests. The build stage compiles your application and creates artifacts. The deploy stage pushes the built artifacts to your hosting environment.\n\nDependencies between stages use the needs keyword to ensure they run in order. For example, the test job needs needs: lint, and the deploy job needs needs: [test, build]. This creates a pipeline where code quality is verified before testing, tests must pass before building, and building must succeed before deployment.\n\nMatrix builds multiply your test matrix across combinations of operating systems and language versions. Define a matrix strategy with os and version variables, and GitHub Actions automatically creates a job for each combination. For example, testing on ubuntu-latest, windows-latest, and macos-latest across Node.js 18, 20, and 22 generates 9 parallel jobs. This verifies compatibility across all target environments without writing separate workflows." },
      { heading: "Environment Management and Secrets", body: "GitHub Actions provides environment management through environments, secrets, and variables. Environments group related configuration for deployment targets like staging and production. Each environment can have approval reviewers — requiring manual approval before deployment proceeds. Environments also track deployment history with timestamps and commit references.\n\nSecrets store sensitive values like API keys, cloud credentials, and tokens. Organization secrets are available to all repositories in the organization, repository secrets are scoped to one repository, and environment secrets override repository secrets for specific environments. Secrets are encrypted with libsodium and are not visible in logs — they are masked when accidentally printed.\n\nBest practices for secrets include using OpenID Connect (OIDC) instead of long-lived credentials where possible, rotating secrets regularly, using short-lived tokens, and never hardcoding secrets in workflow files. GitHub's OIDC integration with AWS, Azure, GCP, and HashiCorp Vault lets workflows request temporary credentials directly without storing any cloud secrets." },
      { heading: "Workflow Validation and Debugging", body: "Workflow files are YAML, and YAML errors are the most common cause of workflow failures. Missing spaces, incorrect indentation, and tabs instead of spaces all cause parse errors that prevent the workflow from starting. GitHub shows these errors on the Actions tab but the messages can be cryptic. Always validate your YAML before pushing.\n\nFor debugging, enable diagnostic logging by setting the ACTIONS_STEP_DEBUG secret to true. This adds verbose output for runner diagnostics, action resolution, and step execution. You can also use the tmate action to SSH into a running runner for interactive debugging, though this should only be used in development repositories for security reasons.\n\nOur GitHub Actions Validator checks your workflow YAML syntax, validates action references against the marketplace, and catches common mistakes like incorrect indentation, invalid trigger syntax, and missing required fields. Paste your workflow file and get immediate feedback. All validation runs client-side in your browser — no data is uploaded." },
    ],
  },
  "jwt-refresh-tokens": {
    title: "JWT Refresh Token Rotation: Implementation Guide",
    description: "A practical guide to JWT refresh tokens — token rotation, expiration strategies, secure storage, and preventing replay attacks.",
    date: new Date("2026-07-04"),
    sections: [
      { heading: "Why Refresh Tokens?", body: "JWT access tokens have a fundamental limitation: they expire. Short expiration times (15-60 minutes) limit the damage if a token is stolen, but require the user to re-authenticate frequently. Long expiration times are convenient but increase security risk. Refresh tokens solve this by providing a long-lived credential that can request new short-lived access tokens without requiring the user to log in again.\n\nThe refresh token pattern improves security through separation of concerns. Access tokens are sent with every API request and have a short lifespan. Refresh tokens are stored securely and used only to obtain new access tokens. If an access token is stolen, its short expiration limits the window of vulnerability. If a refresh token is stolen, the original can be rotated and invalidated.\n\nOur JWT Expiration Checker helps you inspect both access and refresh tokens to verify their claims, expiration times, and structure. This is invaluable during development when you need to debug token lifecycle issues or verify that your authentication flow is working correctly." },
      { heading: "Access Token vs Refresh Token Architecture", body: "The standard JWT authentication flow uses two tokens with different lifetimes and purposes. The access token is short-lived (typically 15 minutes to 1 hour) and contains claims about the user's identity and permissions. It is sent in the Authorization header of every API request. The refresh token is long-lived (typically 7-30 days) and is used exclusively to obtain new access tokens.\n\nWhen the access token expires, the client sends the refresh token to a dedicated endpoint (POST /auth/refresh) which validates the refresh token and returns a new access token. This happens transparently to the user — the client intercepts 401 responses, refreshes the token, and retries the original request. The user only needs to log in when the refresh token itself expires.\n\nThe key architectural decision is where to store each token. Access tokens can be stored in memory (JavaScript variable) or in an httpOnly cookie. Refresh tokens should always be stored in an httpOnly cookie with Secure and SameSite=Strict flags. Never store refresh tokens in localStorage — they are accessible to any JavaScript running on your domain, making them vulnerable to XSS attacks." },
      { heading: "Refresh Token Rotation Strategy", body: "Refresh token rotation is a security pattern where each refresh request invalidates the old refresh token and issues a new one. This means a refresh token can only be used once. If an attacker steals a refresh token and the legitimate user also uses it, one of them will attempt to use an already-rotated token, triggering a security alert.\n\nImplementation of rotation requires a database or cache to track active refresh tokens. When a refresh request arrives, look up the token, verify it's still valid, issue a new access token AND a new refresh token, then invalidate the old refresh token by removing it from the database. If a rotated token is presented again, this indicates token theft — revoke all tokens for that user and require re-authentication.\n\nReplay detection is the companion to rotation. Maintain a family of tokens — a chain linking each new token back to its original. If a token from outside the current family is presented, the entire family is compromised. This prevents an attacker who steals a single token from maintaining access indefinitely. Our JWT Decoder helps you inspect the claims in your tokens to verify family identifiers and rotation status." },
      { heading: "Common Refresh Token Implementation Mistakes", body: "The most common mistake is storing refresh tokens in the same database table as access tokens without distinguishing their purpose. Refresh tokens need different storage considerations — they must be revocable, support rotation, and have configurable lifetimes independent of access token policies. Always use separate storage or at minimum a type field to distinguish them.\n\nAnother frequent error is not validating the refresh token's association with the user. When a refresh request comes in, verify that the token belongs to the requesting user (match sub claim against authenticated user ID). Also validate that the token hasn't been revoked due to password change, account suspension, or administrative action.\n\nFailing to implement absolute expiration is another critical mistake. Even with rotation, refresh tokens should have a hard maximum lifetime (typically 30-90 days) after which the user must re-authenticate regardless of rotation activity. This limits the window of exposure if an attacker maintains access through a compromised family of tokens. Use our JWT Expiration Checker to verify your token expiration policies during development and testing." },
      { heading: "JWT Refresh Token Best Practices", body: "Use httpOnly, Secure, SameSite=Strict cookies for refresh token storage. This prevents XSS attacks from reading the token and CSRF attacks from using it. Set the cookie path to /auth/refresh so it is only sent to the refresh endpoint, not to every API request. This reduces the attack surface if an attacker finds a way to make requests from the user's browser.\n\nImplement token rotation with a rolling expiration window. Each refresh resets the refresh token's expiration, but cap the total lifetime. For example, refresh tokens expire 7 days from the last refresh, but never more than 30 days from initial issuance. This balances security with user convenience — active users rarely need to log in, but inactive sessions are cleaned up automatically.\n\nMonitor and alert on unusual refresh patterns. Multiple refresh attempts from different IP addresses, refreshes for revoked tokens, and refresh attempts outside normal geographic regions all indicate potential compromise. Log refresh token operations with enough detail to investigate incidents. Use our JWT Decoder during development to inspect token claims and verify your implementation is correct before deploying to production." },
    ],
  },
  "wi-fi-qr-codes": {
    title: "Wi-Fi QR Codes: Generate and Share Network Access",
    description: "Learn how Wi-Fi QR codes work, how to generate them for WPA2 and WPA3 networks, and best practices for secure sharing.",
    date: new Date("2026-07-05"),
    sections: [
      { heading: "What Is a Wi-Fi QR Code?", body: "A Wi-Fi QR code is a special type of QR code that stores Wi-Fi network credentials in a standardized format. When scanned with a smartphone camera, the phone automatically recognizes the Wi-Fi configuration and prompts the user to connect — no need to type SSIDs or passwords manually. This makes Wi-Fi QR codes ideal for guest networks, cafes, hotels, and offices.\n\nThe Wi-Fi QR code format is defined by the WiFi Alliance and encoded as a string starting with WIFI:. The format includes the authentication type (WPA, WPA2, WPA3, or nopass), the SSID (network name), the password, and whether the network is hidden. A complete string looks like: WIFI:T:WPA;S:MyNetwork;P:MyPassword;;\n\nOur Wi-Fi QR Code Generator creates these codes instantly in your browser. Enter your network details and get a downloadable QR code image in SVG format. All generation happens client-side — your network credentials never leave your computer. This is important because a Wi-Fi password is a permanent credential that should not be transmitted to third-party services." },
      { heading: "Wi-Fi QR Code Format Specification", body: "The Wi-Fi QR code format follows a strict syntax that all modern smartphones understand. The format string has five possible fields, each separated by semicolons. T specifies the authentication type: WPA, WPA2, WPA3, or nopass (open network). S specifies the SSID (network name). P specifies the password. H specifies whether the network is hidden (true or false). The string ends with two semicolons.\n\nEach field value must be escaped according to specific rules. Backslashes are escaped as double backslashes, semicolons as backslash-semicolon, commas as backslash-comma, and colons as backslash-colon. This escaping ensures that special characters in SSIDs or passwords don't break the format. For example, a network named \"Guest: Free\" becomes S:Guest\\: Free in the QR code string.\n\nAuthentication type determines what security protocol the phone uses to connect. WPA2 is the most widely supported type and works with virtually all devices manufactured since 2006. WPA3 is the current standard but newer devices only. For mixed environments, WPA2/WPA3 transitional mode provides the broadest compatibility. Open networks use nopass — use these cautiously as traffic is unencrypted." },
      { heading: "Generating Wi-Fi QR Codes", body: "Generating a Wi-Fi QR code requires two steps: constructing the WIFI: formatted string and encoding it into a QR code image. The format string must exactly match the specification — any syntax error causes the phone to reject the code. Common mistakes include omitting the trailing semicolons, using wrong authentication type codes, and not escaping special characters in SSIDs.\n\nOur Wi-Fi QR Code Generator handles all formatting automatically. Enter your network name (SSID), select the security type, enter the password, and optionally mark the network as hidden. The tool constructs the correct format string and generates a QR code. You can download the result as an SVG file that prints cleanly at any size — ideal for printing on labels, posters, or business cards.\n\nThe QR code size affects scan reliability. A code printed at 3x3 cm (1.2 inches) scans reliably from 30 cm (12 inches). For larger viewing distances like wall posters, make the code proportionally larger — 10x10 cm scans from about 1 meter away. Always test your printed QR code with multiple phones before mass production." },
      { heading: "Wi-Fi QR Code Security Considerations", body: "Wi-Fi QR codes present a unique security challenge: the network password is encoded in plain text within the QR code. Anyone who can photograph the code has the password permanently. Treat printed Wi-Fi QR codes like printed passwords — limit distribution and consider rotating the password periodically.\n\nFor business environments, create separate guest network SSIDs with their own QR codes. This keeps the main corporate network password private while providing convenient guest access. Set guest networks to use client isolation so guests cannot see each other's traffic. Most enterprise access points support multiple SSIDs with different security policies.\n\nWPA3 provides forward secrecy — even if someone captures the QR code and the encrypted Wi-Fi traffic, they cannot decrypt past sessions after the password is changed. Consider rotating guest Wi-Fi passwords monthly and reprinting QR codes. Our Wi-Fi QR Code Generator makes regeneration quick — just enter the new password and download a fresh code." },
      { heading: "Using WebUtil's Wi-Fi QR Code Generator", body: "Our free Wi-Fi QR Code Generator creates QR codes for any Wi-Fi network type. The interface is straightforward: enter your SSID, select authentication type (WPA, WPA2, WPA3, or none), enter the password, and optionally mark the network as hidden. The QR code updates in real-time as you type.\n\nThe generator outputs SVG files that scale infinitely without quality loss. This is important for printing — a PNG QR code at 300 DPI works for small prints, but SVG guarantees sharp results at any size. You can print the code on a standard printer, upload to a print service for professional signs, or embed it in digital documents.\n\nAll processing runs entirely in your browser using the Canvas API and QR code generation library. Your network credentials never leave your device. The tool works offline after the initial page load, making it suitable for air-gapped environments where network credentials cannot be transmitted over the internet." },
    ],
  },
  "word-counter-guide": {
    title: "Word Counter Guide: Track Writing Metrics Like a Pro",
    description: "A complete guide to word counting — why word count matters for SEO, technical writing, and content creation. Learn reading time estimation.",
    date: new Date("2026-07-06"),
    sections: [
      { heading: "Why Word Count Matters", body: "Word count is one of the most fundamental metrics in writing, yet it is surprisingly important across many professional contexts. For SEO and content marketing, search engines use content length as a quality signal — comprehensive articles that thoroughly cover a topic tend to rank higher than short, superficial content. Studies consistently show that the average first-page result on Google contains at least 1,500 words.\n\nFor technical writers and documentation authors, word count helps estimate reading time and documentation coverage. API documentation with comprehensive coverage naturally contains more words but also communicates more information. Reading time estimates help readers decide whether they have time to consume the content now or should bookmark it for later.\n\nFor students and academics, word count is often a strict requirement — essays, papers, and theses must fall within specified ranges. A reliable word counter helps writers hit these targets precisely without the distraction of manual counting. Our Word & Character Counter provides instant word, character, sentence, and paragraph counts with reading time estimates." },
      { heading: "Word Count vs Character Count vs Other Metrics", body: "Different metrics serve different purposes. Word count measures the number of whitespace-delimited tokens in a text. It is the standard metric for content length in publishing, education, and SEO. Character count measures every character including spaces and punctuation. Social media platforms like Twitter (X) use character counts to enforce post length limits.\n\nReading time is derived from word count using an average reading speed of 200-250 words per minute for technical content. This estimates how long it takes someone to read the text. Speaking time is similar but uses a slower rate of 130-160 words per minute for spoken delivery. Sentence count helps assess readability — very long sentences reduce comprehension, while very short sentences can feel choppy.\n\nParagraph count helps evaluate document structure. A healthy ratio is 3-5 sentences per paragraph. Paragraphs that are too long discourage reading on screens, while single-sentence paragraphs can indicate insufficient development of ideas. Our Word & Character Counter provides all these metrics simultaneously, giving you a comprehensive view of your text's structure and readability." },
      { heading: "Using Word Count for SEO Content Strategy", body: "Content length directly correlates with search engine ranking for informational queries. Analysis of millions of search results shows that content length is one of the top ranking factors for non-commercial queries. However, padding content with filler words hurts both readability and rankings — Google's algorithms detect thin or repetitive content and may penalize it.\n\nThe sweet spot for blog posts depends on the topic depth. How-to guides and tutorials perform well at 1,500-2,500 words. Comprehensive guides and cornerstone content should target 3,000-5,000 words. News and update posts can be shorter at 500-1,000 words. The key is matching content length to the depth required by the topic — a short post about a simple topic ranks fine if it fully answers the user's question.\n\nReadability is as important as length. Content written at a grade 8-10 reading level performs best for general developer audiences. Use shorter sentences, active voice, and clear transitions between sections. Your word count target should be driven by comprehensive topic coverage, not by hitting an arbitrary number. Our Word & Character Counter helps you monitor both length and reading level." },
      { heading: "Word Counting in Different Languages", body: "Word counting algorithms vary across languages. In English and most European languages, words are separated by spaces, making whitespace-based counting straightforward. CJK languages (Chinese, Japanese, Korean) do not use spaces between words, requiring dictionary-based or statistical segmentation for accurate word counts. Character counts are often used instead for these languages.\n\nUnicode normalization matters for accurate counting. Characters with combining marks (like accented characters or emoji sequences) can be represented in multiple ways — as a single composed character or as a base character plus combining marks. Normalization to NFC or NFD form ensures consistent counting across different input methods.\n\nLine endings and special characters also affect counts. CRLF vs LF line endings, tabs, non-breaking spaces, and zero-width characters should all be handled correctly. Our Word Counter handles these edge cases by normalizing input before counting, ensuring consistent results regardless of how the text was entered or copied from other applications." },
      { heading: "Using WebUtil's Word & Character Counter", body: "Our free Word & Character Counter provides instant text analysis as you type or paste. The counter displays word count, character count (with and without spaces), sentence count, paragraph count, and estimated reading time. All metrics update in real-time as you edit the text.\n\nThe tool handles edge cases automatically. URLs, email addresses, and code snippets are counted correctly without breaking the word count. Non-printing characters are filtered out for accurate counting. The reading time estimate uses 200 words per minute for English text, which is appropriate for average reading comprehension.\n\nAll processing happens client-side in your browser. No text is uploaded to any server — your content remains private. This is essential for writers working on confidential documents, drafts, or proprietary content. The tool works offline after the initial page load and can handle large documents of 100,000+ words without performance issues." },
    ],
  },
  "remove-duplicate-lines-guide": {
    title: "Remove Duplicate Lines: Complete Developer's Guide",
    description: "Learn how to remove duplicate lines from text — deduplication strategies, sorting, command-line alternatives, and online tools.",
    date: new Date("2026-07-07"),
    sections: [
      { heading: "Why Remove Duplicate Lines?", body: "Duplicate lines in text files are a common problem that causes data analysis errors, inflated file sizes, and processing inefficiencies. Log files accumulate repeated error messages that make identifying new issues harder. CSV exports from databases often contain duplicate rows due to join operations or multiple data sources. Configuration files can have duplicate entries after repeated modifications or merges.\n\nRemoving duplicates is essential for data cleaning workflows. Deduplicated data produces accurate statistics — duplicate entries skew counts, averages, and other aggregations. Clean data is also more compressible, reducing storage and bandwidth costs. For log analysis, deduplication highlights unique events that need attention rather than repeating the same message thousands of times.\n\nOur Remove Duplicate Lines tool provides instant deduplication with sorting options. The tool runs entirely in your browser, making it suitable for sensitive data that cannot be shared with external services. Whether you are cleaning a data export, preparing a mailing list, or deduplicating log files, the tool handles files of any size efficiently." },
      { heading: "Deduplication Strategies", body: "There are several approaches to removing duplicate lines, each suited to different use cases. The simplest strategy is exact line matching — if two lines are identical, keep only the first occurrence. This works well for most text files and preserves the original order of unique lines. It is the default behavior of most deduplication tools including ours.\n\nCase-insensitive deduplication treats lines that differ only in capitalization as duplicates. 'ERROR: Timeout' and 'error: timeout' would be considered the same line. This is useful for log files where the same message might appear in different cases depending on the logging system. Our tool supports case-insensitive mode for these scenarios.\n\nWhitespace-tolerant deduplication normalizes spaces before comparing, treating 'ERROR  Connection refused' and 'ERROR Connection refused' as duplicates. This helps when files have inconsistent spacing due to different editors or copy-paste from formatted sources. Combined with sorting, these strategies give you fine-grained control over what counts as a duplicate." },
      { heading: "Command-Line Alternatives", body: "Unix and Linux systems provide several command-line tools for removing duplicate lines. The classic approach uses sort and uniq: sort input.txt | uniq > output.txt. The sort command orders lines alphabetically, and uniq removes adjacent duplicates. This is fast and efficient for large files but changes the line order, which may not be acceptable for some use cases.\n\nTo preserve the original order of unique lines, use awk: awk '!seen[$0]++' input.txt > output.txt. This uses an associative array to track seen lines and only prints the first occurrence. It is slower than sort | uniq for very large files but preserves the file's original sequence, which matters for logs and chronological data.\n\nFor files where specific fields determine uniqueness, use awk with field-based deduplication: awk '!seen[$1]++' input.txt removes lines where the first field has been seen before. This field-aware approach is useful for CSV files where the same ID might appear in multiple rows with different data. Our Remove Duplicate Lines online tool provides these options through a simple interface without requiring command-line expertise." },
      { heading: "Handling Large Datasets", body: "Deduplicating very large files requires memory-efficient approaches. For files that fit in memory (typically under 500MB on modern systems), the hash-based approach works well — store a hash of each line in a Set and skip duplicates. This is O(n) time complexity and requires memory proportional to the number of unique lines.\n\nFor files larger than available memory, external sorting is required. Split the file into chunks, sort each chunk, then perform a merge pass that deduplicates while merging. This is the same approach used by the Unix sort command with the -u flag. Our tool processes files in the browser using streaming chunked operations that handle files up to 100MB without freezing the interface.\n\nBrowser-based deduplication has limitations compared to native tools for very large files. The browser's memory limit and single-threaded JavaScript mean that files over 500MB should be processed with command-line tools. For most common use cases — log files, CSV exports, configuration files — the browser-based approach is fast, convenient, and keeps your data private." },
      { heading: "Using WebUtil's Remove Duplicate Lines Tool", body: "Our free Remove Duplicate Lines tool provides instant text deduplication with customizable options. Paste your text, choose your deduplication strategy, and get cleaned output immediately. The tool supports case-sensitive and case-insensitive matching, whitespace normalization, and optional sorting alphabetically or by line length.\n\nThe interface shows original line count and deduplicated line count side by side, giving you immediate feedback on how many duplicates were removed. You can download the result as a text file or copy it to your clipboard. For lists where order matters, preserve the original sequence. For logs, sort alphabetically to group related messages.\n\nAll processing runs client-side in your browser. Your data never leaves your machine, making the tool safe for sensitive information like password lists, customer data, or proprietary logs. No sign-up required, no usage limits, and no data retention. The tool works offline and handles text of any length efficiently." },
    ],
  },
};

export const blogToolMapping: Record<string, string> = {
  "json-formatting-guide": "json-formatter",
  "common-json-errors": "json-formatter",
  "url-encoding-decoding": "url-encoder",
  "base64-encoding-methods": "base64-encoder",
  "password-security-best-practices": "password-generator",
  "color-converter-guide": "color-converter",
  "uuid-guide": "uuid-generator",
  "online-tools-security": "jwt-decoder",
  "json-vs-xml": "json-formatter",
  "json-schema-guide": "json-formatter",
  "how-apis-use-json": "json-formatter",
  "hash-generator-guide": "hash-generator",
  "sql-formatting-guide": "sql-formatter",
  "xml-formatting-guide": "xml-formatter",
  "yaml-converter-guide": "yaml-converter",
  "html-entities-guide": "html-entities",
  "case-converter-guide": "case-converter",
  "markdown-guide": "markdown-preview",
  "text-diff-guide": "text-diff",
  "image-converter-guide": "image-converter",
  "lorem-ipsum-guide": "lorem-ipsum",
  "jwt-authentication-explained": "jwt-expiration-checker",
  "json-vs-yaml-for-apis": "yaml-converter",
  "uuid-v4-vs-uuid-v7": "uuid-v7-generator",
  "regex-tutorial": "regex-tester",
  "css-units-guide": "px-to-rem",
  "docker-compose-tutorial": "docker-compose-validator",
  "rest-api-design": "json-formatter",
  "how-qr-codes-work": "qr-code-generator",
  "common-jwt-errors": "jwt-decoder",
  "json-compact-pretty-print": "json-formatter",
  "json-to-csv-guide": "json-to-csv",
  "kubernetes-yaml-guide": "kubernetes-yaml-validator",
  "github-actions-guide": "github-actions-validator",
  "jwt-refresh-tokens": "jwt-expiration-checker",
  "wi-fi-qr-codes": "wi-fi-qr-code-generator",
  "word-counter-guide": "word-counter",
  "remove-duplicate-lines-guide": "remove-duplicate-lines",
};

export function getBlogPost(slug: string): BlogContent | undefined {
  return blogContent[slug];
}

export function getBlogPosts(): BlogPost[] {
  return blogPosts;
}

export function getToolLink(slug: string): string {
  return blogToolMapping[slug] || slug;
}
